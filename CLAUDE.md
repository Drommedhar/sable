# Sable — project guide for Claude

Sable = cross-platform (Windows/Linux/macOS) **raster** image editor, Photoshop/Affinity look-and-feel, GPU-first, with local AI (selection, repair, upscale, generative fill).

**Full design doc: [PLAN.md](PLAN.md). Read it before any architectural work.**

## Locked decisions (see PLAN.md §1)
- **UI**: AvaloniaUI for chrome only. Canvas = separate GPU surface embedded via `NativeControlHost`. Never render the canvas with Avalonia/Skia.
- **GPU**: GPU-first, **wgpu** (one API → DX12/Vulkan/Metal). Spike uses `Silk.NET.WebGPU` binding.
- **AI**: light tier (SAM2/BiRefNet/ESRGAN/LaMa) = in-process ONNX Runtime, ships with app, no Python. Generative tier (SD/Flux/Qwen via **Diffusers**, Apache-2.0) = opt-in install, `uv` venv sidecar over IPC. **No ComfyUI.**
- **AI compute**: GPU-only, no CPU fallback. Block op if VRAM won't fit.
- **Models**: user-provided weights only, no bundled catalog.
- **License**: MIT. Keep GPL out. ImageSharp split-license = watch.
- **Effects are layers**: adjustments + live filters are first-class tree nodes (Affinity model), also attachable as per-layer FX. EVERYTHING non-destructive + undoable — enforced by the graph/compositor, not per-feature. New effect MUST be a graph node (serializable params + WGSL pass + undo entry).
- **Name**: Sable. Native format `.sable`. Repo folder still `CrossDraw/` — rename later.

## Architecture invariants (do not break)
- Document = graph, not pixel buffer. On-screen image is always a recompute by the GPU compositor.
- Tiled layer storage (256×256), GPU-resident, dirty-tile undo snapshots.
- Working space: linear float (RGBA16F/32F); ICC convert at boundaries.
- Engine (`Sable.Engine`) is UI-agnostic + headless-testable. MVVM (CommunityToolkit.Mvvm) for UI↔engine.

## Module layout (PLAN.md §7)
`Sable.App` (Avalonia shell) · `Sable.UI` · `Sable.Canvas` (GPU host) · `Sable.Engine` (doc/layer/compositor graph) · `Sable.Gpu` (wgpu binding + WGSL) · `Sable.Imaging` (codecs/color/tiling/IO) · `Sable.Tools` · `Sable.Ai` (ONNX) · `Sable.Ai.Sidecar` (Diffusers) · `Sable.Format` (.sable IO/history) · `Sable.Core` (math/color/undo/settings).

## Current state
- Solution scaffolded: `Sable.slnx` + 11 `Sable.*` projects under `src/` + `Sable.Gpu.Spike` console. TFM **net10.0** (SDK 10), Avalonia 12.0.4, Silk.NET.WebGPU 2.23.0.
- **M0 spike #1 PASS**: `Sable.Gpu.Spike` composites 2 RGBA8 layers with a WGSL Normal-blend compute pass on the GPU, reads back, writes `spike_out.png`. Verified on AMD Radeon PRO / Vulkan. Proves wgpu binding + compute + readback.
- Avalonia shell (`Sable.App`) runs: dark PS/Affinity layout skeleton (menu/options bar/tool strip/Color+Layers panels) per §13. Static — no docking lib yet.
- **M0 spike #2 PASS**: `Sable.Canvas/GpuSurfaceControl.cs` embeds a live wgpu swapchain in Avalonia via `NativeControlHost` (Win32 HWND → `InstanceCreateSurface` → configure → present loop on a 16ms `DispatcherTimer`). **Clears the #1 architectural risk (GPU canvas composited into Avalonia, not Skia).** Windows-only so far.
- **M0 spike #3 PASS**: `Sable.Gpu/BlendDemoRenderer.cs` runs the blend compute → rgba8 texture (`CopyBufferToTexture`) → fullscreen-triangle blit (`fullscreen_blit.wgsl`) → swapchain. Real composited pixels render live inside the Avalonia canvas (verified visually). Full GPU loop closed: compute → texture → render → present → embedded.
- **M1 engine — first slice DONE**: real document model + N-layer GPU compositor driving the embedded canvas.
  - `Sable.Core`: `BlendMode` (FULL set, 30: PS + Affinity extras — int values are the WGSL contract; `composite.wgsl` `blend()` switch incl. non-separable Hue/Sat/Colour/Lum via W3C helpers), `Undo/UndoStack` + `IUndoableCommand`.
  - `Sable.Engine`: `Document` (bottom→top `Layers`, `CreateDemo()`), `Layers/Layer` (abstract: opacity/blend/visible/dirty), `PixelLayer` (full-res RGBA8). `Compositing/GpuCompositor` walks layers, blends via `composite.wgsl` ping-ponging two storage buffers, copies result→texture. Recomposites only when `doc.AnyDirty`.
  - `Sable.Gpu`: `SurfaceBlitter` (reusable fullscreen blit), `WgpuDevice.CreateWgslModule`, `Shaders/composite.wgsl`.
  - `GpuSurfaceControl` now renders a `Document` via compositor+blitter. Verified: 3-layer demo (gradient + red disc + Screen highlight) composites correctly in-window.
- **M1 layers panel bound (DONE)**: `Sable.UI/ViewModels/{DocumentViewModel,LayerViewModel}` (CommunityToolkit.Mvvm). MainWindow shares one `Document` between `GpuSurfaceControl` and the VM. Layer rows (ListBox), visibility checkbox, blend ComboBox, opacity slider are two-way bound; setters flag `layer.Dirty` → canvas recomposites next tick. Verified: panel shows real layers (Highlight/Red Disc/Background) with correct blend/opacity.
- **M1 PNG import/export (DONE)**: `Sable.Imaging/ImageCodec` (SkiaSharp decode/encode RGBA8), `Sable.Engine/IO/DocumentIO` (OpenImage→Document, ExportPng). `GpuCompositor.CompositeToBytes` flattens on GPU + reads back. File menu: Open Image / Export PNG via Avalonia StorageProvider. **Present step now uses a `present_copy.wgsl` compute `textureStore`** (not CopyBufferToTexture) so document width is unconstrained — verified at 643px (non-256-aligned) export.
- **M1 layer ops + undo (DONE)**: `Sable.Engine/Commands/LayerCommands` (Add/Remove/Move, all `IUndoableCommand`). `DocumentViewModel` runs them through its `UndoStack`, resyncs the layer list on `Undo.Changed`, exposes `[RelayCommand]` NewLayer/DeleteLayer/MoveLayerUp/MoveLayerDown/UndoEdit/RedoEdit. Edit▸Undo/Redo (Ctrl+Z/Y) bound. (Footer reworked to Affinity layout — see below; Move/Undo/Redo dropped from footer, use drag-drop + Ctrl+Z/Y.) `Document.NeedsComposite`/`MarkStructureChanged` drive recomposite on structural change. Undo/redo logic verified headlessly (add→move→undo→undo→redo exact).
- **M1 viewport (DONE)**: `Sable.Gpu/ViewportTransform.Fit` (aspect-fit + zoom + pan, pure/tested: 512² in 1000×800 → ox=100,scale=1.5625). `fullscreen_blit.wgsl` maps surface pixels → doc UV, checkerboard outside doc, composites layer alpha over checker. `GpuSurfaceControl` holds zoom/pan + `ZoomBy/PanBy/ResetView`. Input: Window keyboard (+/-/0, arrows) guaranteed; transparent overlay handles mouse wheel/drag best-effort (**native-HWND airspace may swallow mouse over the canvas** — keyboard is the reliable path; revisit with a proper input strategy).
- **M1 brush tool (DONE)**: `Sable.Tools/BrushTool` (soft round dab, src-over into PixelLayer RGBA8, stroke interpolation). **Airspace solved**: `GpuSurfaceControl.Input.cs` subclasses the native child HWND's WndProc (`SetWindowLongPtrW`/`GWLP_WNDPROC`), captures WM_LBUTTON*/MOUSEMOVE, maps surface px → doc px via inverse `ViewportTransform`, drives the brush, marks layer dirty → live recomposite. `ActiveLayer` set from the selected layer. **Verified live** (synthetic WndProc strokes painted white onto the canvas through the full path).
- **M1 tiling + paint undo (DONE)**: `PixelLayer` has 256² tile accessors (`GetTile`/`SetTile`, edge-aware). `Sable.Tools/StrokeSession` snapshots touched tiles copy-on-first-touch across a gesture; `PaintLayerCommand` holds before/after tile bytes (bounded memory). `GpuSurfaceControl.CommandProduced` → routed to the VM `UndoStack` in MainWindow, so brush strokes are undoable on the same stack as layer ops. `Ctrl+Z`/`Ctrl+Y`/`Ctrl+Shift+Z` KeyBindings added (Avalonia MenuItem InputGesture is display-only). **Verified live**: paint stroke → Ctrl+Z reverts it. Headless: painted=25207 → undo=0 → redo=25207.
- **M1 `.sable` save/load (DONE)**: `Sable.Format/SableFile` — zip container, `document.json` (size + layer params) + `layers/{i}.raw` (RGBA8, deflate). `ZipArchive` + `System.Text.Json`, no deps. File menu: Open (Ctrl+O) / Open Image / Save (Ctrl+S) / Save As / Export PNG; `_currentPath` tracks Save vs Save As. Verified round-trip: size/layer-count/blend/opacity/pixels preserved.
- **M1 adjustment layer (DONE)** — first effects-are-layers node (PLAN §5): `Sable.Engine/Layers/AdjustmentLayer` (Brightness/Contrast, no pixels). Compositor walks it like any layer and runs `adjust_bc.wgsl` over the accumulated backdrop below (opacity = strength); `DispatchAdjust` + `_adjPipeline`. VM: `LayerViewModel.IsAdjustment`/`Brightness`/`Contrast`, `DocumentViewModel.NewAdjustmentCommand`; UI footer ◑ button + Brightness/Contrast sliders shown when an adjustment is selected. Serialized in `.sable` (Type="adjustment"). Verified: visible contrast boost, 4-layer demo, round-trips. **New adjustment = AdjustmentKind + WGSL pass + the compositor branch + serializer case.**
- **M1 per-layer masks (DONE)**: `Layer.Mask` (RGBA8, R channel = coverage; `AddWhiteMask`/`RemoveMask`/`HasMask`/`MaskDirty`). Compositor binds a mask buffer to both blend + adjust passes (composite.wgsl binding 5, adjust_bc.wgsl binding 4); pixel alpha *= mask, adjustment strength *= mask. Layers without a mask use a shared full-size white buffer (`_whiteMask`). Mask buffers cached (`_maskBuffers`) + serialized in `.sable` (`masks/{i}.raw`). Verified: demo disc masked by a vertical gradient fades top→bottom; round-trips.
- **M1 mask painting + viewport fixes (DONE)**: BrushTool/StrokeSession/`PaintRasterCommand` generalized to paint any RGBA8 `(byte[], w, h)` target (tile ops moved to `RasterTiles`); brush paints layer pixels OR mask. `GpuSurfaceControl.PaintMask` toggle (key **M**) — auto-adds a white mask, paints black = hide; undoable on the same stack. **Pan + zoom now in the WndProc subclass** (reliable native path): middle-drag = pan, wheel = zoom; **zoom anchors to the cursor** (`ZoomAt`); blitter sampler **MagFilter=Nearest** so zoomed-in pixels are crisp (Linear minify). Keyboard +/-/0/arrows still work. Verified: pan live (image shifts); headless brush/undo on byte[] (1184px, 25207→0→25207). Mask-paint + zoom-to-cursor logic in but not screenshot-verified (busy desktop blocks reliable window capture — user verifies live).
- **M1 brush color/size UI (DONE)**: Avalonia `ColorView` (HSV picker, `Avalonia.Controls.ColorPicker` pkg + Fluent theme include) in the Color panel → sets `Canvas.Brush.R/G/B`; brush-size slider in the options bar → `Brush.Radius` (slider = diameter). Code-behind handlers (`OnBrushColorChanged`/`OnBrushSizeChanged`), swatch preview. Brush color now drives both pixel paint and mask paint (black on mask = hide, white = reveal) — removed the hardcoded color overrides. Build clean (ColorView theme loads, no crash); not screenshot-verified (desktop blocks capture — user verifies live).
- **M1 eraser + eyedropper (DONE)**: `BrushTool.Erase` (destination-out: reduces alpha, keeps color); `GpuSurfaceControl.EraseMode` toggle (key **E**), undoable like paint. Eyedropper = **Alt+click** (`GetKeyState` VK_MENU) samples `ActiveLayer.Pixels` → sets brush color + raises `ColorPicked` → updates the ColorView + swatch. Verified headless: erase 1968→844 px (center fully cleared, soft edges partial).
- **M1 clip + adjustments + fill + partial-upload (DONE, all unit-tested — 37 tests)**:
  - **Clip-to-layer**: `Layer.ClipToBelow`; composite.wgsl `params.clip` multiplies coverage by backdrop alpha; UI checkbox; serialized.
  - **Adjustments unified**: `adjust.wgsl` switches on `kind` (BrightnessContrast/Levels/HSL/**Curves**/Exposure/Vibrance/Threshold/Posterise/Invert) with generic p0..p5; `AdjustmentLayer.PackParams`; Image▸Add Adjustment menu; serialized. **Recipe: new adjustment = AdjustmentKind + a case in adjust.wgsl + PackParams + toolbox sliders + serializer fields.** Single-param ops (Exposure=stops/2^x, Vibrance=HSL sat boost, Threshold=luma cut, Posterise=quantise, Invert=1-rgb) are cases 4-8; B&W (R/G/B weights) + White Balance (temp/tint) are 9-10; **Colour Balance** (case 11, shadow/mid/high RGB shifts, p0..p8) + **Channel Mixer** (case 12, 3×3 row-major p0..p8) are the multi-param ones. **Adj uniform buffer is now 64B = kind+opacity+p0..p11 (+2 pad)**: `_adjParamsBuf`=64, `prm` stackalloc uint[16], `QueueWriteBuffer` 64, `DispatchAdjust` Size=64, `adjust.wgsl` Adj struct p0..p11. PackParams gets a Span<float>(…, 12). New ≤12-param adjustment now needs no buffer change. (stackalloc size must match buffer or 0xC0000409.)
  - **Curves (DONE, Phase 1 #2)**: `AdjustmentLayer.Curves` = 4 channels (0=RGB composite,1=R,2=G,3=B), each a sorted point list; `BuildLut` → 4×256 f32, uploaded to `GpuCompositor._curveLutBuf` (storage, `adjust.wgsl` binding 5), shader case 3u samples per-channel then composite. Bespoke `CurveEditor : Control` (click=add, drag=shape, right-click=delete, RGB/R/G/B tabs) in `AdjustmentWindow` (code-behind syncs target on selection change). Serialized as point lists. **Recipe for LUT-based adjustments (e.g. Gradient Map): reuse `_curveLutBuf` + a `kind` case rather than the 6 scalar params.**
  - **Fill opacity (DONE, Phase 1 #1)**: `Layer.FillOpacity` (0..1, scales pixels only — PS "Fill"); composite.wgsl `params.fillOpacity` (Params now has it in the slot formerly `_p0`) multiplies source alpha; layer-panel Fill slider (`LayerViewModel.FillOpacityPercent`); serialized. **Blend-If deferred** (user chose skip for now).
  - **Adjustment params = modeless toolbox** (`AdjustmentWindow.axaml`, NOT in the layer panel): floating window bound to the same `DocumentViewModel`, shows per-kind sliders for the selected adjustment layer. Auto-shows when an adjustment layer is selected; toggle via **Window ▸ Adjustments**; closing hides (not destroys). Layer-row representation of filters is deliberately unchanged (will be redone later).
  - **XAML `SelectedIndex`/`IsChecked` set in markup fires the change event DURING `InitializeComponent`** — before the `x:Name` code-behind fields are wired. A `SelectionChanged`/`ValueChanged` handler that touches `this.SomeNamedControl` or a field set after `InitializeComponent()` will NPE on dialog open (hit on `SettingsWindow.OnCategory`). Guard handlers with an early-return (`if (_field is null/empty) return;`) like `ExportDialog.Recompute`; rely on the markup's default `IsVisible` for the initial state instead of the init-time event.
  - **Checkbox size**: Fluent's CheckBox box was oversized vs Affinity. Fixed with a **bespoke `ControlTheme x:Key="{x:Type CheckBox}"`** in `App.axaml` (15px box from `CheckBoxBoxSize` token, dark, blue when checked). Do NOT try to shrink Fluent's checkbox via `/template/ Border#NormalRectangle` part selectors — those part names are **not present in Avalonia 12.0.4's compiled theme** (verified by grepping the Fluent dll), so the selectors silently no-op. A full ControlTheme is the version-proof way.
  - **Theme tokens (DONE)**: `src/Sable.App/Theme.axaml` = ResourceDictionary, **single source of truth for UI sizing** (font scale, bar/control/row heights, tool+icon button sizes, slider geometry + brushes, color-wheel max). Merged into `App.axaml` via `ResourceInclude`; styles + inline sizes consume tokens via `{StaticResource <Name>}`. **Tune the whole UI density here.** Tuned denser to match Affinity. NOTE: GridLength col/row defs (e.g. `ColumnDefinitions="40,*,300"`) stay literal — a `double` token won't convert to `GridLength`. **`{StaticResource}` works inside Style Setters + inline `Height=`, verified at runtime.**
  - **Layer FX (DONE, Phase 1 #3)**: per-layer non-destructive `Layer.Effects` (`LayerEffect`: Kind=DropShadow/OuterGlow/Stroke/ColorOverlay, colour/opacity/blend/radius/offset/size/strokePos). Compositor `BlendContentWithFx` wraps each content layer (pixel/shape/text/group): rasterise layer to doc space (`_fxLdoc`), composite shadow+glow BEHIND, the layer, then overlay+stroke FRONT. `fx.wgsl` mode0=tint(colour×layer-alpha), mode1=stroke(dilate/erode alpha, outside/inside/centre), mode2=inner-shadow, mode3=inner-glow (disc-sample nearby transparency, clipped inside the layer alpha; offset baked in shader), mode4=gradient-overlay (linear, angle, clipped), mode5=bevel/emboss (alpha-edge gradient lit by angle; highlight=rgb, shadow=rgb2, depth=offX); outer shadow/glow reuse `blur.wgsl`; sprites blended via `BlendBufferInto`. `_fxParamsBuf`=**48B** (mode,rgb,size,pos,rgb2,angle,offX,offY) + `_fxLdoc`/`_fxTint`/`_fxBlur`. **Front FX render in `Layer.Effects` list order** (behind = shadow/glow by category) → `LayerViewModel.MoveEffect(kind,dir)` reorders within the front/behind group; EffectsWindow has Up/Down buttons. **8 effects done (1.3 complete); 3D out-of-scope.** Dedicated modeless **`EffectsWindow`** — **Affinity master-detail**: left `ListBox` of effects (enable checkboxes), right param panel for the selected row (blend-mode combo + gradient sliders + hex colour with live swatch via `HexToBrushConverter`), bottom Fill-Opacity + Close. Opened from the **footer "fx" button** (`OnFxButton`) or Window▸Layer Effects. `LayerViewModel` Fx proxies (Has*/…/per-effect *Blend). Serialized (`EffectDto`). **Recipe: new FX = LayerEffectKind + fx.wgsl branch (or reuse tint+blur) + a BlendContentWithFx step + EffectsWindow rows + EffectDto.** GPU smoke: `Sable.Gpu.Spike` adds all 4 to the disc.
  - **Settings / session (DONE, Phase 2 #3)**: `Sable.Core.Settings.SableSettings` (theme/DPI/auto-update/RecentFiles/OpenTabs/window placement) + `SettingsService` (JSON at `%AppData%/Sable/settings.json`). `MainWindow` loads in ctor → `ApplySettings` (window bounds + `ApplyTheme` + recent menu), `Opened`→`RestoreSession` (reopen saved `.sable` tabs), `Closing`→save bounds + open-tab paths. `NoteRecent(path)` on open/save → **File▸Open Recent** (`RecentMenu` rebuilt in code). **Affinity-style `SettingsWindow`** (Edit▸Preferences) — search + category sidebar (General/UI/Performance/Colour/ML/Updates/About) + grouped right pane, **pill `ToggleSwitch`** (bespoke ControlTheme in App.axaml — needs `PART_MovingKnobs` Panel + `PART_SwitchKnob` Panel, Avalonia slides them; can't just style Fluent's). `SettingsWindow(settings, gpuName)` edits the POCO in place, OK commits. **Theme = setting + `RequestedThemeVariant` only (Fluent); custom chrome stays hardcoded dark — gray/light chrome needs tokenising inline colours (TODO).**
  - **Multi-document tabs (DONE, Phase 2 #1)**: `DocumentTab` (Doc + own `DocumentViewModel`/undo + Path/Title/IsDirty/IsActive; dirty set on `Vm.Undo.Changed`). `MainWindow._tabs` (`ObservableCollection`) bound to `TabStrip` ItemsControl (tab template + × close + `+` new; active highlight via `BoolToTabBgConverter`). `OpenInNewTab(doc,path,title)` creates+activates; `ActivateTab` swaps `Canvas.Document`+`DataContext`+`WireCanvas` (per-tab callbacks). **No demo at startup** — `EmptyState` overlay until first doc. `NewDocumentWindow` (size/DPI/presets), File▸New-from-Clipboard (`ReadOsImage`), Open/OpenImage → new tab, **file drag-drop** onto the window (Avalonia 12: `e.DataTransfer.TryGetFiles()` + `DataFormat.File`, NOT the old `e.Data`/`DataFormats.Files`). Close prompts when dirty (`ConfirmWindow.Ask`). Ctrl+N new, Ctrl+W close. Save updates the active tab's Path/Title/IsDirty.
  - **Brush polish (DONE, Phase 1 #7)**: Hardness + Flow sliders in the options bar (`Brush.Hardness`/`Flow`). **HUD adjust**: Ctrl+Alt + left-drag = size (horizontal) / hardness (vertical) — `_hudAdjust` branch in `GpuSurfaceControl` input (intercept in `OnLeftDown` before painting; `BrushAdjusted` → `MainWindow.SyncBrushSliders`). Affinity feel: preview ring stays anchored at the drag-start point (`_lastMouseX/Y` pinned to `_hudStart`), and the **OS cursor is hidden during the drag + warped back to the start on release** via new `IInputSource.HideCursor()`/`RestoreCursor()` (Win32 `GetCursorPos`/`SetCursorPos`/`ShowCursor` in `WindowsInputSource`; no-op in `NullInputSource`). **Pencil** (B-group tool): `Brush.Pencil` → binary `cov = dist<=r ? 1 : 0` (no smoothstep). **Eyedropper options**: `EyedropperRadius` (0/1/2 = point/3×3/5×5 average) + `EyedropperAllLayers` (samples `ReadComposite` vs active layer); options-bar combo + checkbox. Gradient radial/conical = user-deferred (gradient flagged incorrect).
  - **Layer collapse ops (DONE, Phase 1 #6 part)**: merge-down/merge-visible/stamp/flatten/rasterise. `GpuSurfaceControl.RenderLayersToPixels(List<Layer>)` builds a temp `Document` of those layers + `compositor.CompositeToBytes` → RGBA8; `MainWindow.Collapse` wraps it in a `PixelLayer.SetBuffer`. `ReplaceLayersCommand(doc, parent, remove, insertIndex, newLayer)` swaps N→1 (undo re-inserts the removed at original indices; stamp uses `AddLayerCommand`). Layer menu + Ctrl+E (down) / Ctrl+Shift+E (visible) / Ctrl+Shift+Alt+E (stamp) in `OnGlobalKeyDown`. **Locks**: `Layer.LockPosition/LockPixels/LockAlpha` — Move gated in input (`!ml.LockPosition`), paint/fill blocked (`CreateSession`/`DoFill` return when `LockPixels`), `BrushTool.LockAlpha` tints rgb but preserves alpha. Panel header toggles. **Colour tags**: `Layer.ColorTag` 0-7, `LayerViewModel.TagBrushFor`/`TagSwatch`, row strip + 8-swatch picker. **Between-row reorder**: `DocumentViewModel.DropLayerRelative(dragged,target,above)` (index-corrected `MoveLayerToCommand`; UI top→bottom vs model bottom→top, "above"=ti+1); `MainWindow` DnD shows a blue `DropIndicator` line, group-middle band = drop-into. **All serialized. #6 DONE.**
  - **Clipboard (DONE, Phase 1 #5 — internal)**: `Layer.Clone()` + `protected abstract CreateClone()` per type (deep copy pixels/params/mask/effects/children). `Sable.Engine.Clipboard.SableClipboard` (static: region `Pixels`/`Width`/`Height` OR a whole `Layer`). `DocumentViewModel.DuplicateLayer` (Ctrl+J, undoable AddLayerCommand of a clone) + `PasteLayer`. `GpuSurfaceControl.CopyRegion()` (selected region of active layer, mask-clipped, or whole layer) + `CopyMerged()` (composite cropped to selection). `MainWindow` orchestrates Copy/Cut/CopyMerged/Paste/PasteInto (Edit menu + Ctrl+C/X/V/Shift+C/Shift+V/J in `OnGlobalKeyDown` — MenuItem InputGesture is display-only). Cut = copy + `DeleteSelection`. PasteInto = region + selection as the new layer's mask. **OS clipboard image** = `WriteOsImage`/`ReadOsImage` via Avalonia 12 `ClipboardExtensions.SetBitmapAsync(IClipboard,Bitmap)` / `TryGetBitmapAsync(IClipboard)` — **these extensions live in `Avalonia.Input.Platform` (needs that `using`)**; PNG round-trips through `ImageCodec.EncodePngBytes`/`DecodeRgbaBytes` + Avalonia `Bitmap`. Copy writes the region to the OS clipboard; Paste falls back to `TryGetBitmapAsync` when `SableClipboard` is empty (images from other apps paste in).
  - **Live filters (DONE, Phase 1 #4)**: 10 `FilterKind` — Gaussian/Box/Motion/Zoom blur, Sharpen, Unsharp, HighPass, Clarity, AddNoise, Denoise. `FilterLayer` params Radius/Amount/Angle. Compositor `RenderFilter(flt, src, dst)` filters the backdrop into `_fxTint`, then **`DispatchFilterMix(current, _fxTint, other, opacity, mask)` REPLACES the backdrop with the filtered result** (crossfade by opacity × mask via `filter_mix.wgsl` + `_mixBgl`/`_mixPipeline`/`_mixParamsBuf`, 6 bindings). **A live filter SHOWS the filtered version — it must NOT src-over composite over the original** (the old `BlendBufferInto(_fxTint over backdrop)` left the original's sharp edges visible through a blur's soft edges = bug). Opacity/mask = the crossfade amount; fill/clip don't apply to filters. Shaders: `blur.wgsl` (box flag), `filter_dir.wgsl` (motion/zoom), `filter_conv.wgsl` (sharpen), `filter_combine.wgsl` (unsharp/highpass/clarity — 2 inputs, own `_combineBgl`), `filter_noise.wgsl` (add-noise/bilateral-denoise). dir/conv/noise reuse `_blurBgl` (4-binding) via `MakeComputePipeline(shader, bgl)` + shared `_filterParamsBuf`(32B). `BlurInto(src,dst,radius,box)` helper. Toolbox (AdjustmentWindow) shows Radius/Amount/Angle gated by `FilterUsesRadius/Amount/Angle`. **Recipe: new filter = FilterKind + a shader (or reuse) + a RenderFilter case + FilterUses* flags + menu + serialize.**
  - **Affinity-style layers footer (DONE)**: decluttered to New-layer · Mask (`ToggleMaskCommand` add/remove white mask on selected layer) · **Adjustments flyout** (half-circle button → `MenuFlyout` of all 11 adjustments) · **Live Filters flyout** (Gaussian Blur) · Group · Delete (far right). Top-menu **Image▸Add Adjustment removed** (flyout is the only entry point now). Move-up/down + Undo/Redo removed from footer. **Ungroup currently has no footer/menu trigger — only `UngroupCommand` (used by drag-drop); re-add a UI entry if needed.**
  - **Affinity-style adjustment panel (DONE)**: `AdjustmentWindow` = header (title + **Reset**, `LayerViewModel.ResetAdjustment`) → per-kind content → footer (**Opacity + Blend Mode** combo). **`GradientSlider`** (`Sable.App`, bespoke `Control`, static gradient track + thumb, two-way `Value`) used with a **numeric value box** per row; HSL hue=rainbow, sat=ramp, lum/levels=grey gradients (brushes in the window's `Window.Resources`). **Histogram**: `Histogram.Compute(rgba)→int[768]` + `Histogram.Draw`; drawn behind Curves (`CurveEditor.SetHistogram`, channel-masked) and above Levels (`HistogramView`). Data via `AdjustmentWindow.CompositeProvider` set in `MainWindow.ShowAdjustmentWindow` to `Canvas.ReadComposite()` — **whole-composite approx, backdrop-below is TODO**. **Output Levels** = `AdjustmentLayer.OutBlack/OutWhite` → `adjust.wgsl` Levels case p3/p4 remap; serialized.
  - **Fill/bucket**: `Sable.Tools/FillTool.Flood` (scanline 4-connected, tolerance), key **F**, click-to-fill, undoable (whole-layer tile snapshot).
  - **Partial GPU upload**: `Layer.DirtyTiles` (set) + `MarkTilesDirty`; brush/fill/PaintRasterCommand report touched tiles via `Action<IReadOnlyCollection<(int,int)>>`; `GpuCompositor.GetLayerBuffer` uploads only dirty tiles row-by-row (new buffer / no-tile-info → full upload). Mask still full-upload.
- **M1 live filter — Gaussian blur (DONE)**: `Sable.Engine/Layers/FilterLayer` (FilterKind, Radius) — non-pixel node, blurs the backdrop below it. `blur.wgsl` separable Gaussian (premultiplied), 2 dispatches H+V via `_filterTemp`; compositor `FilterLayer` branch + `DispatchBlur`. Filter menu ▸ Gaussian Blur; toolbox Radius slider (shown for `IsFilter`); serialized. Verified: 38 tests + GPU smoke (5-layer composite visibly blurred). **Recipe: new live filter = FilterKind + WGSL pass(es) + compositor branch + toolbox sliders + serializer.** NOTE: blur ignores mask/opacity for now (applies fully) — mask/opacity-for-filters is a follow-up.
- **Effects toolbox**: `IsEffect` (adjustment OR filter) drives the modeless `AdjustmentWindow`; closing main quits app (`ShutdownMode.OnMainWindowClose`); toolbox is a plain window (no cancel-on-close — that vetoed the owned-parent close).
- **M1 layer groups (DONE)**: `GroupLayer : Layer { List<Layer> Children }` — document is now a tree. Compositor recurses: `CompositeList(list, depth)` composites a group's children into a scratch buffer (pool `_scratch`, ping-pong pair per depth via `ScratchPair`), then `BlendInto` blends the group result with its blend/opacity/mask (**isolated** grouping; pass-through later). `Document.FindParent` + recursive `NeedsComposite`/`ClearDirty`. Parent-aware commands: `AddLayerCommand(doc, parent, layer, index)`, `RemoveLayerCommand`/`MoveLayerCommand(doc, layer, delta)` (operate within parent), `GroupCommand`/`UngroupCommand` (undoable). VM flattens the tree top→bottom with `Depth`/`Indent`/`IsGroup`; footer 🗀 group / ⊟ ungroup buttons. Recursive `.sable` serialize (LayerDto.Children + global entry counter). Verified: 41 tests (group/ungroup/FindParent/nested-serialize) + GPU smoke (6-layer composite incl group). **Deferred: drag-drop into groups (use group/ungroup + move), pass-through groups, multi-select grouping.**
- **M1 multi-select + drag-drop grouping (DONE)**: ListBox `SelectionMode=Multiple` → `DocumentViewModel.SetSelection`; **Group** groups the whole selection (`GroupLayersCommand`, order-preserving). Drag-drop = **manual pointer DnD** (Avalonia 12 reworked the DataObject/DragDrop API, so avoided it): press→move(≥5px)→release; target resolved by `LayerList.InputHitTest(releasePos)` walking the visual tree (NOT `e.Source` — the list captures the pointer so e.Source stays the source row). Drag ghost overlay (Canvas in a root Panel) follows the cursor.
- **Nested-child effects + drag-drop + docked param panels (DONE)** — Affinity nested-filter model + the three follow-ups the user asked for:
  - **Children moved to base `Layer`** (`Layer.Children`/`HasChildren`): ANY layer can hold children. For a `GroupLayer` they ARE the content; for a content layer (pixel/shape/text/path) they are **nested effect layers (live filters / adjustments) clipped to that layer only** — so a filter inside a layer affects ONLY it, while a top-level filter still affects the whole composite below. `GroupLayer.Children` removed (inherits base); base `Clone()` deep-copies children. Generalized every tree walk to recurse `l.Children` (not just `GroupLayer`): `Document.AnyDirty`/`ClearDirty`/`FindParent`, compositor `CollectLive`/`SubtreeDirty`, `CropCommand`/`ResizeCommand` Capture/Apply.
  - **Compositor**: `BlendOneLayer` early-returns for a content layer with children → `RenderContentWithChildren(layer, mask, depth)` renders own content into a doc-space scratch (`ScratchPair(depth+1)`, transform+mask baked, Normal/op=1) then composites the child stack on top via recursive `BlendOneLayer` (child filter/adjustment uses the existing backdrop path = parent content + lower children), then `BlendDocContentWithFx` applies the parent blend/opacity/fill/clip + FX. FX loops extracted to reusable `ApplyBehindFx`/`ApplyFrontFx` (read `_fxLdoc`); `GetContentBuffer` mirrors the per-type source pick (handles brush-preview dab on pixel layers). **Recipe for any layer-with-children = render-own-content → child stack → FX → blend.**
  - **Serialize**: `SableFile` saves/loads `Children` uniformly for EVERY type (dropped the group-only loop); `LoadGroup` is now just the typed shell.
  - **VM/UI**: `DocumentViewModel.AddTree`/`ToggleExpand` + `_collapsed` (HashSet<Layer>) work for any `HasChildren`; chevron binds `LayerViewModel.HasChildren` (was IsGroup). **Adding a filter/adjustment NESTS into the selected content layer** (`AddEffectLayer`, Affinity); deselect / select a group to add top-level.
  - **Drag-drop reworked** (`MainWindow` `_dragModels` multi-set captured at press; `DropIntoBox` highlight for the middle band): top/bottom band = reorder (`DropMultipleRelative`), middle band = `DocumentViewModel.DropOnto` → onto a group = move in; **all-effect drag onto a content layer = nest as children**; **else auto-group target+dragged**. Multi-selection drags as a set. (Replaced the old `DropLayer`/`_dropIntoGroup`.)
  - **Delayed-deselect for multi-select drag** (`_pendingCollapse`): Avalonia's ListBox collapses a multi-selection to the clicked row *on press* (no modifier), which killed dragging the whole selection. Fix: in `OnLayerPointerPressed`, when a plain press lands on a row already in a multi-selection (and NOT on a child control — `PressOnInteractive` skips eye/chevron/tag/slider/combo/textbox), set `e.Handled=true` + `e.Pointer.Capture(LayerList)` so the selection survives the press; on release WITHOUT a drag, collapse to that row (Explorer/Photoshop behaviour). Shift/Ctrl presses are never suppressed (ListBox extends/toggles normally).
  - **Adjustment/Filter params + Shape properties moved into the RIGHT PANEL** (user: "place overlay in right panel instead of modal window"): new reusable `AdjustmentPanel`/`ShapePanel` UserControls (single source of truth) docked **Bottom** in the Layers panel (below the layer list + footer), shown via `IsVisible="{Binding SelectedLayer.IsEffect/IsShape, FallbackValue=False}"` (MaxHeight 400). `AdjustmentWindow`/`ShapeWindow` are now thin wrappers hosting the same panels (Window▸Adjustments still works); auto-show on selection removed. `AdjPanel.CompositeProvider = Canvas.ReadComposite` set in ctor for the Curves/Levels histogram.
  - Verified: 265 tests (incl. nested-child serialize roundtrip + FindParent + Clone) + GPU smoke (`nested filter blur: edgeA=28 farA=0 centreA=255` — blur clipped to the layer) + app launches. **Deferred: child pixel/group layers nest but transform applies in doc space (after, not before, child filters); multi-parent auto-group is multiple undo steps.**
- **Dynamic layer bounds (DONE, Phase 1.1)** — layers are no longer forced to document size; each `PixelLayer` has its own `Width/Height` + `OffsetX/Y` (buffer top-left in doc), so off-canvas pixels are preserved and the display clips to the canvas (output grid stays doc-sized). **Compositor is per-layer-size aware**: `composite.wgsl` `Dims{width,height,srcW,srcH}` (output grid vs source buffer), `srcTexel`/`maskTexel` index by `srcW/srcH`; `DocToLayer(srcW,srcH,…)` pivots on the layer (offset = buffer position). `DispatchBlend` rewrites `_dimsBuf` per layer + binds `src`/`mask` at `srcW*srcH*4`; `GetLayerBuffer`/`GetMaskBuffer` alloc at the layer's own bytes (reallocate on grow via `_layerBufferBytes`/`_maskBufferBytes`). **`params.hasMask`** (composite.wgsl) — no-mask layers skip the mask read entirely (a doc-sized white mask would index out-of-bounds for an *oversized* layer); `DispatchBlend` binds `src` as a dummy when `mask==null`. **Masks are layer-aligned** (same dims as the layer; `ExpandToCover` grows the mask too). **Paint is origin-aware**: `BrushTool.OriginX/Y` + `FillTool` origin + `StrokeSession` origin map doc↔buffer so the doc-space selection clip/mask still line up; `PixelLayer.ExpandToCover(docW,docH)` grows a sub-doc/offset layer to cover the canvas at paint-start (preserves content, keeps it undoable-ish via tile snapshots). Paste/place keeps the region size+offset (no clip) except paste-into (doc-sized for mask alignment). `.sable` stores `LayerW/LayerH` (0 = legacy doc-sized). `CornersSurface`/`PixelLayer.ContentBounds` use layer dims for the gizmo/move overlay. **Auto-crop to content (DONE)**: `PixelLayer.TrimToContent()` shrinks the buffer to the tight non-transparent bbox (adjusts offset; 1×1 if fully empty) — the inverse of `ExpandToCover`. A pixel-paint gesture = `ExpandToCover(doc)` at press → paint → `TrimToContent()` at release, recorded as one **`RasterStateCommand`** (whole-raster before/after = buffer+W/H+offset, content-sized, undo-safe across resize; `RasterState.Capture`/`ApplyTo`). Brush + bucket fill use this; **mask paint keeps tile-diff `PaintRasterCommand`** (masks don't auto-crop). So painted layers hug their content but you can still paint anywhere on the canvas (each stroke re-expands). **Live brush dab-preview is layer-aware** (`DispatchStamp` writes the layer dims into `_dimsBuf`, dab centre mapped to buffer space `Cx-OffsetX`, `_previewBuf` grows via `EnsurePreviewBuffer` for oversized layers) — works on any layer bounds (verified: offset-layer preview dab lands correctly in the GPU smoke). Verified: GPU smoke (sub-doc masked layer composites right-visible/left-masked/outside-transparent; offset-layer preview dab) + 116 tests (`ExpandToCover`, `TrimToContent`, `RasterStateCommand` resize-roundtrip, brush origin clip, sub-doc `.sable` roundtrip). **Remaining: non-destructive document crop (keep cropped pixels); painting beyond the canvas edge (`ExpandToCover` only grows to the doc union).**
- **Navigation/overlays (DONE, Phase 2.5)**: zoom UI + live status bar (`Canvas.EffectiveScale`/`SetZoomPercent`/`ViewChanged`/`CursorDocMoved`; status-bar Fit/1:1/zoom-% + doc info + cursor coords), **grid + pixel-grid** (drawn in `fullscreen_blit.wgsl`, reused the `_p4/_p5/_p13` uniform pads → `gridOn/gridSp/pixGrid`), **rulers** (`Controls/Ruler.cs`, Avalonia, in chrome cells flush with the surface — **airspace-safe because they're NOT over the native HWND**; fed `Canvas.ViewportDip` = device-px viewport ÷ `TopLevel.RenderScaling`; theme via `ActualThemeVariant` + `ActualThemeVariantChanged`→`InvalidateVisual`, else lookups fail → black), **guides + smart guides** drawn via **storage buffers bound into the blit shader** (binding 4 = guides cyan, binding 5 = smart-guides magenta; layout `[countX,countY,_,_,Xs...,Ys...]`, cap 254/axis) — this is the pattern for **variable-count canvas overlays** (the fixed vp uniform is full). Guides interaction: ruler-click create (airspace blocks drag-from-ruler), canvas WndProc drag to move / drop-outside to delete. `SmartSnap`/`SnapAxis` in `GpuSurfaceControl.Input.cs`.
- **Gotcha**: the GPU canvas control has `x:Name="Canvas"`, which shadows `Avalonia.Controls.Canvas` in code-behind — fully-qualify `Avalonia.Controls.Canvas.SetLeft/SetTop`.
- **Custom title bar (menu-in-header, DONE)**: `MainWindow` ctor sets `WindowDecorations = WindowDecorations.BorderOnly` (Avalonia **12** — the old `ExtendClientAreaChromeHints`/`ExtendClientAreaToDecorationsHint` enum was **removed in v12**, replaced by the `WindowDecorations` enum `None`/`BorderOnly`/`Full` + `WindowDecorationsElementRole` attached roles). BorderOnly keeps the OS resize border + shadow, drops the native caption. The menu `DockPanel` (row 0) is the title bar: logo (Left) · `Menu` (Left) · caption buttons `StackPanel` (Right, `Button.winbtn` min/max/close, `winclose` hover red) · a transparent `TitleDrag` Border (last child fills) with `OnTitleBarPressed`→`BeginMoveDrag` + `OnTitleBarDoubleTapped`→toggle maximise. Root grid `Margin="{Binding $parent[Window].OffScreenMargin}"` prevents clipping when maximised.
- **Gotcha (Avalonia style selectors are EXACT-type)**: `Selector="Button.tool"` matches only controls whose runtime type is *exactly* `Button` — **NOT subclasses**. A custom `ToolButton : Button` gets none of that style (size/margin/hover all silently ignored — symptom: "my margin edits change nothing"). Use **`:is(Button).tool`** to match a type + its subclasses. Same for the templated variant: `:is(Button).tool:pointerover /template/ ContentPresenter`. (Bit us on the reusable `Controls/ToolButton.cs` — the tool strip's `Button.tool` style stopped applying once the buttons became `ToolButton`s.)
- **M1 remaining**: between-row drop reorder + indicator, pass-through groups, more adjustments (Curves/colour balance) + filters (sharpen/unsharp), partial MASK upload, mask/opacity for filters, vector/text. Minor: doc-swap leaks old GPU buffers; HiDPI 1:1; mouse input Windows-only.
- **Controls**: wheel=zoom-to-cursor · middle-drag=pan · 0=fit · +/-=zoom · arrows=pan · left-drag=paint · **K**=paint layer mask · **Q**=quick-mask · **M**=Marquee tool group · **E**=erase · **P**=pen/node (cycles; pen: click=node drag=handles Enter=finish Esc=cancel; node: drag=edit Alt-click=delete click-path=insert) · **Alt+click**=eyedropper · **X**=swap fg/bg · **D**=reset fg/bg · Ctrl+Z/Y=undo/redo · Ctrl+O/S=open/save.

## Tools (M2, PLAN §14)
- **Tool framework**: `Sable.Tools/ToolKind` (Move/Marquee/Brush/Eraser/Fill/Eyedropper/Hand/Zoom). `GpuSurfaceControl.ActiveTool` routes WndProc left-button via `OnLeftDown`/`OnLeftUp` switch. Toolbar buttons (`Tag` + `OnSelectTool`, highlight) + key shortcuts (V/M/B/E/G/I/H/Z, K=edit-mask). Options bar shows active tool (`ToolStatus`).
- **Move + Transform = ONE tool (V)**: Move and Transform unified (no separate Transform tool; V group has only Move). On a **pixel layer** the Move tool shows the full transform gizmo (`GpuSurfaceControl.cs` gizmo overlay gated on `ActiveTool==Move`); `BeginMoveTool`→`BeginTransform` for pixel, plain `_moving` offset for non-pixel. Non-destructive `Layer.OffsetX/Y` (composite.wgsl samples src+mask at `-offset`); `MoveOffsetCommand`/`TransformLayerCommand` undoable; serialized.
  - **Gizmo handle/modifier model** (Affinity-style, `ScaleByHandle` in `GpuSurfaceControl.Input.cs`): corner/edge handle drag = **uniform** scale (aspect-locked) anchored at the OPPOSITE handle by default; **Shift** = non-uniform (dragged axis/axes only); **Ctrl** = scale from CENTRE; **Ctrl+Shift** = non-uniform from centre. Scale pivots about the layer centre then the offset is recomputed so the anchor point stays fixed (un-rotates the cursor delta, solves `S·v`, projects for uniform). **Rotate** = top handle, **Shift** snaps 15°. **Move** (interior/anywhere) **Shift** = axis-constrain. **Alt-drag a corner** = perspective/free-distort (moved off Ctrl, which is now centre-scale). Modifiers are read live each drag frame (`_lastMods`), so holding/releasing mid-drag works. NEW gizmo state: `_xfHandle` (0..3 corner / 10..13 edge), `_xfBoxW/H`; replaced the old edge-normal/`_xfStartDist` fields + `BeginEdge`. **Scale math is interactive → verify live** (not headless).
- Existing brush/eraser/fill/eyedropper rerouted through tools. Eraser = Brush.Erase; Alt+click = quick eyedropper in paint tools. Hand=left-drag pan, Zoom=click (Alt=out), wheel=zoom-to-cursor, middle-drag=pan.
- **Pending tools** (PLAN §14.4): Marquee selection (rect/ellipse, GIMP-style in-canvas move/resize grips), Lasso, Magic Wand, Crop, Gradient, Clone, Heal, Dodge/Burn, Shapes, Type, Pen. Flyout grouping + per-tool options bar to follow.
- **Selection combine modes (DONE)**: Shift=add, Alt=subtract, Shift+Alt=intersect, none=replace — rect/ellipse/lasso/wand. `Selections.Combine`/`Rect` + `Document.SnapshotSelectionMask`; gesture wiring `CaptureSelMode`/`ApplyMask` in `GpuSurfaceControl.Input.cs`. Plain-rect Replace keeps grips; any modifier rasterizes to a mask.
- **Pen / vector path (DONE, Phase 4 #1)**: `Sable.Engine/Layers/PathLayer` = cubic-bézier `PathNode` list (anchor + in/out handles, abs doc px) + `Closed` + fill (`FillR/G/B/A`,`Filled`) + stroke (`StrokeR/G/B/A`,`StrokeWidth`,`Stroked`). `Rasterize` = `Flatten()` (each segment → 16-24 line steps) → 4×-vertical-supersampled even-odd scanline fill + distance-to-polyline stroke (over). Compositor `GetPathBuffer` mirrors `GetShapeBuffer`. **Pen tool** (`ToolKind.Pen`, key **P**) in `GpuSurfaceControl.Pen.cs`: click=corner node, drag=mirrored smooth handles (`PenDown/PenMove/PenUp`), click-first-node or Enter=`CommitPen` (closed→filled / open→stroked, brush colour), Esc=`CancelPen`; leaving the tool auto-commits. **Live preview via a NEW blit storage buffer (binding 6)** — `pen` array `[nodeN,activeIdx,flatN,_, nodes(6×n), flatPts(2×m)]` surface px; `fullscreen_blit.wgsl` draws the flattened curve spine + handle lines + anchor squares + handle dots. **This is the pattern for variable-count vector overlays** (markers + a flattened polyline). Uniform grew 224→**240B** (`VpFloats=60`, `penOn` at u[56]) — BGL/bind-group now 7 entries. Serialized in `.sable` (`type="path"`, `PathNodes` flat float[7×n]). Verified: 6 path raster tests + serialize roundtrip + GPU smoke (filled+stroked bézier diamond composites).
- **Node tool (DONE, Phase 4 #2)**: `ToolKind.Node` (P group, P cycles Pen/Node) in `GpuSurfaceControl.Node.cs` — edits the selected `PathLayer`. Hit-test (handles first, then anchors; offset-aware via `OffsetX/Y`): drag anchor=move node+handles, drag handle=reshape (smooth mirrors opposite, **Alt breaks** the mirror via `Smooth`), **Alt-click anchor=delete** (kept ≥2 nodes), **click on the path=insert node** (`TryInsertOnPath` = nearest segment + de Casteljau split, curve-preserving). Each gesture = one undoable `EditPathCommand` (whole node-list before/after + closed flag). Overlay = `BuildNodeOverlay` (reuses the pen binding-6 buffer; PenActive = dragged idx). Verified: `EditPathCommand` do/undo test + build/launch.
- **Shape depth (DONE, Phase 4 #3)**: shared `Sable.Engine/Layers/VectorRaster` (static: even-odd 4×-supersampled `Fill` + dash-aware distance-field `Stroke`, both src-over straight-alpha) — used by BOTH `PathLayer` and `ShapeLayer` (PathLayer.Rasterize now delegates to it). `ShapeLayer` v2: `Filled`/fill `R/G/B/A` + `Stroked`/`StrokeR/G/B/A`/`StrokeWidth` + dash (`DashOn`/`DashLen`/`GapLen`) + per-kind params (`CornerRadius`, `Sides`, `InnerRatio`); new kinds **RoundedRect/Polygon/Star/Arrow** (enum appended after Line to keep old serialized ints valid). `BuildOutline()` returns the polyline+closed flag (arcs for rounded rect, n-gon/star vertices, ellipse 64-seg); Arrow = stroked shaft + filled head triangle. New `ToolKind.ShapeRoundedRect/ShapePolygon/ShapeStar/ShapeArrow` (U-group cycle). Draw-time defaults via `Sable.Tools/ShapeStyle` (`Canvas.Shape`) set by the **options-bar `ShapeOpts`** (fill/stroke/width/dash + sides/inner/corner, per-kind visibility in `UpdateOptionsBar`). **Modeless `ShapeWindow`** (Shape properties panel) auto-shows when a shape layer is selected — bound to `DocumentViewModel`, edits via `LayerViewModel.Shape*` proxies (`ShapeFilled`/`ShapeFillHex`/`ShapeStroked`/`ShapeStrokeHex`/`ShapeStrokeWidth`/`ShapeDashOn`/`ShapeSides`/`ShapeInnerPercent`/`ShapeCornerRadius`, each sets `model.Dirty` → live recomposite). Serialized (all new shape fields). Verified: 7 shape raster tests + roundtrip + GPU smoke (rounded-rect dashed / hexagon / star / arrow all composite). **Recipe: new shape = ShapeKind + BuildOutline case + ToolKind + options-bar/panel rows + serializer fields.**
- **Text depth (DONE, Phase 4 #4)**: `TextLayer` gains `BoxWidth` (0=point text; >0 = **area text** word-wrapped to that width — `TextRaster.LayoutLines` greedy wrap) + `Tracking` (letter spacing; `TextRaster` lays glyphs per-char when tracking≠0) + `PathPoints` (baked doc-px polyline for **text-on-path**). `TextRaster.RenderOnPath` places glyphs by arc length along the polyline, each rotated to the local tangent, into a bitmap covering the path bbox (returns its own ox/oy origin). **Text→curves**: `TextLayer.ToPath()` → `Sable.Imaging/TextOutline.Glyphs` extracts each glyph's SKPath into bézier contours (raw-iterator verb walk; quad/conic→cubic) returned as neutral `OutlineContour`/`OutlineNode` (Imaging stays engine-independent), built into a `PathLayer` (first contour = primary `Nodes`, rest = `ExtraContours`). **`PathLayer.ExtraContours`** (extra sub-paths) + **`VectorRaster.FillMulti`** (even-odd across ALL contours at once → counters/holes work, e.g. "O"/"a"); `Rasterize` fills all closed contours together, strokes each. Pen/Node tools still edit only the primary sub-path. **Type menu** (Convert to Curves / Fit Text to Path / Detach) — `ReplaceLayersCommand` for curves, `SetTextPathCommand` (undoable PathPoints swap) for on-path; `FindPathSource` picks the topmost Path/Shape layer's flattened outline. Options bar: Box W + Tracking fields (`Canvas.TypeBoxWidth/TypeTracking` defaults + live-edit selected text). Serialized (`TxBoxWidth/TxTracking/TxPath` + `PathExtras`). Verified: 8 tests (wrap/tracking/on-path/ToPath/FillMulti-hole/roundtrips) + GPU smoke (vector "Sa", on-path text, wrapped area text). **Note: text-on-path bakes the path polyline — editing the path afterwards doesn't reflow (re-run Fit).**
- **Stroke joins/caps (DONE, Phase 4 #5 — closes Phase 4)**: `VectorRaster.Stroke` rewritten from a pure distance-field to **body rectangles + explicit join/cap geometry**: `LineCap` (Butt/Round/Square) for open ends + `LineJoin` (Miter/Round/Bevel) for corners (miter falls back to bevel past `miterLimit`=4). Per-pixel coverage = max(segment bands, join discs/triangles/miter-quads, caps). `ShapeLayer.Cap/Join` + `PathLayer.Cap/Join` (default Round/Round = old look); dashed strokes cap each dash, no joins. Shape-panel Cap/Join combos (`LayerViewModel.ShapeCap/ShapeJoin`); serialized (`ShCap/ShJoin`, `PsCap/PsJoin`). Verified: cap (butt vs square) + join (miter vs bevel corner) tests + GPU smoke (miter hexagon + square-cap zigzag). **Phase 4 vector/text COMPLETE; boolean ops / custom shapes / full typography / text styles deferred to §16.16 advanced.**
- **Transform depth (DONE, Phase 5 §16.9)**: layer transform is now full **R·S·K** (rotation·scale·**shear**) about centre — `AffineMath` builds the 2×2 forward then inverts (general inverse), so `Layer.ShearX/ShearY` add skew with NO shader change (composite.wgsl still consumes the 6-float `inv`). `LayerXform` + serialize carry shear. **Edge-handle non-uniform scale**: gizmo grew 4 edge-midpoint handles (`_xfMode==4`, `BeginEdge` stores the outward normal + start half-extent, `TransformDrag` projects the cursor → 1-axis scale); blit gizmo draws edge squares. **Numeric Transform panel** (`TransformWindow`, modeless, auto-shows while the Transform tool is active) — `LayerViewModel.Xf*` proxies (offset/scale%/rotation/shear, live-edit + Dirty). **Flip H/V + Rotate 90/180 + Reset** = Layer▸Transform menu (`ApplyTransform` → one `TransformLayerCommand`). **Align/distribute** = Layer▸Align (`AlignLayersCommand`, batch offset; uses `ContentBounds`+Offset; ≥2 align / ≥3 distribute; works on `DocumentViewModel.SelectionModels`).
- **Retouch (DONE, Phase 5 §16.8)**: `BrushTool.Heal` = clone the source TEXTURE but add `mean(dest)-mean(source)` over the dab (tone match) — `ToolKind.Heal` (Alt-click source) / `ToolKind.SpotHeal` (auto source = one diameter left, no click). **Patch** (`ToolKind.Patch`, `DoPatch`): drag a selection to a source offset → heals the whole selection region tone-matched, undoable (`PaintRasterCommand`). All in the **S tool group** (cycles Clone/Heal/SpotHeal/Patch).
- **Liquify (DONE, Phase 5 §16.9)**: `Sable.Tools/LiquifyTool` — inverse-warp displacement brush (Push/Bloat/Pucker/Twirl), snapshots the dab bbox + bilinear-samples a displaced source. `ToolKind.Liquify` (key **Y**), own gesture in input (`_liquifying`, snapshot→stamp-along-segment→`RasterStateCommand`); options bar mode combo + strength. Destructive on the active pixel layer (non-destructive mesh warp is the follow-up). Verified: 161 tests (shear round-trip, heal tone-match, liquify push) + GPU smoke (sheared layer) + launch.
- **Perspective/distort (DONE, Phase 5 §16.9)**: `composite.wgsl` Params grew 48B→**64B** — the affine `inv` (m00..b1) now doubles as the homography's top 2 rows; added `h6,h7,h8` (perspective row) + a perspective divide (`w = h6·dx+h7·dy+h8`, affine layers pass 0,0,1 → w=1). `Layer.Perspective` + `PerspCorners` (8 floats doc px); `Homography.DocToLayerQuad` solves the 4-corner inverse homography (8×8 Gauss) → packs into `[m00,m01,m10,m11,b0,b1]` + `[h6,h7,1]`. Compositor `LayerInverse(layer)` picks affine vs homography; `WriteBlendParams` takes a `perspRow`. **`_paramsBuf` is now 64B** (NewBuffer/QueueWriteBuffer/bind Size all 64; `prm` stackalloc 16). **Gizmo: Ctrl-drag a corner** = free-distort (`_xfMode==5`, `BeginTransform(ctrl)` inits `PerspCorners` from the affine quad, `CornersSurface` uses them when active); `LayerXform` carries perspective for undo; serialized. Verified: homography corner-mapping test + GPU smoke (perspective quad composites).
- **Mesh warp (DONE, Phase 5 §16.9)**: `Sable.Tools/MeshWarpTool.Warp` — destructive triangle-mesh warp (each `gx×gy` cell = 2 triangles; rasterise the dest triangle, barycentric → source triangle → bilinear sample). `ToolKind.MeshWarp` (Y group, cycles Liquify/MeshWarp) in `GpuSurfaceControl.Mesh.cs`: `BeginMeshWarp` builds a 5×5 grid over the active layer's content bounds, drag points, **Enter applies** (`CommitMeshWarp` → `SetBuffer` + `RasterStateCommand`), Esc cancels; grid markers via the pen/node overlay buffer. Non-destructive GPU mesh = follow-up. Verified: mesh identity + shift test + launch. **Phase 5 COMPLETE.**
- **Colour panel depth (DONE, Phase 6 §16.11)**: `Sable.Core/ColorConvert` (sRGB ↔ HSL / CMYK / LAB, pure, 11 round-trip tests). Colour panel (under the wheel) gains: **alpha slider** (`BrushTool.Alpha` multiplies coverage), **fg/bg swatches** + **swap (X)** / **reset (D)** (`Canvas.BgR/G/B` + `SwapColors`/`ResetColors`; X/D keys), a **mode combo (RGB/HSL/CMYK/LAB)** driving 4 relabeling slider+box rows (`SyncColorEditor`/`ApplyColorFromEditor`, `_syncingColor` guards the wheel↔slider feedback loop; LAB/CMYK use per-mode slider Min/Max), and a **swatches palette** (`SwatchList` WrapPanel, + Add, click-to-pick). `OnBrushColorChanged` syncs the editor when not self-driven. **Histogram panel** = `HistogramView` fed by a 800ms `DispatcherTimer` (`Histogram.Compute(Canvas.ReadComposite())`, only when visible + a doc is open). **Info readout** in the status bar: colour under cursor (active-layer pixel, `InfoSwatch`+`InfoLabel`) + selection size (`SelSizeLabel`), updated in `UpdateCursorLabel`. **Deferred (Phase 6 remaining): gradient/pattern presets, gamut/soft-proof, 16/32-bit + ICC pipeline (engine-wide), PSD/TIFF/EXR/RAW IO, SVG/PDF export.**
- **History panel + command palette + context menus (DONE, Phase 7 §16.13/§16.14)**: `UndoStack` gains `History`/`Cursor`/`JumpTo(target)` (undo/redo until N applied). **`HistoryWindow`** (modeless, Window▸History) lists "Open" + each command `Name`, `SelectedIndex`=cursor, selection→`JumpTo`; **named snapshots** = `DocumentViewModel.CaptureSnapshot`(deep-clones the layer tree) + `RestoreSnapshot` via `RestoreSnapshotCommand` (undoable swap of `doc.Layers`). **`CommandPalette`** (Ctrl+K, borderless centred, `Deactivated`→close): fuzzy-search a `List<(string,Action)>` built in `OnCommandPalette` (~45 file/edit/layer/transform/align/tool/window actions; reuses existing handlers via the shared `_e` RoutedEventArgs). **Layer-row context menu** = `DockPanel.ContextFlyout` `MenuFlyout` (Duplicate/Delete/Merge/Rasterise/Mask/Flip) binding to `$parent[Window].((vm:DocumentViewModel)DataContext).<Command>`. Verified: `JumpTo` test + launch.
- **Customisable canvas-overlay appearance (DONE, Phase 7 §17.1)**: guide / smart-guide / grid / quick-mask colours are now settings, not hardcoded. Blit vp uniform grew **60→72 floats (288B)** — `SurfaceBlitter.VpFloats=72`, colours packed at `u[57..68]` (guide rgb / smart rgb / grid rgb / quick-mask rgb; falls back to built-in defaults when `ov.HasOverlayColors` is false), `fullscreen_blit.wgsl` Viewport struct adds `guideR/G/B,smartR/G/B,gridR/G/B,qmR/G/B,_pad0..2` and reads them in the grid/guide/smart/rubylith branches. `BlitOverlay` carries the 4 colours + `HasOverlayColors`. `GpuSurfaceControl.SetOverlayColors(...)` (0..255 tuples) → fields → `ov` each frame. `SableSettings.GuideColor/SmartGuideColor/GridColor/QuickMaskColor` (hex "#RRGGBB") + `ParseHex`/`*Rgb()` helpers; SettingsWindow UI page "Canvas overlays" (4 `HexColorField`s); `MainWindow.ApplyOverlayColors()` pushes to the canvas on load + after Preferences OK. **Pattern for new canvas-overlay colours: add to the vp uniform tail (keep 16B-aligned) + BlitOverlay + GpuSurfaceControl field + a setting.**
- **Rebindable hotkeys (DONE, Phase 7 §17.1 — command actions only)**: `Sable.Core.Settings/KeyCommands.Catalog` = ~30 rebindable commands (`KeyCommandInfo` id/label/category/default-gesture, gesture strings use Avalonia `KeyGesture.Parse` grammar e.g. `Ctrl+OemPlus`, `Ctrl+D0`). `SableSettings.KeyBindings` (id→gesture override) + `GestureFor(id)` (override ?? default; "" = unbound), serialized (only non-default overrides saved). **MainWindow** owns the id→handler map (`KeyCommandRun`, lambdas reuse existing `OnXxx` via shared `_e`) + `_keyGestures` (parsed `KeyGesture` list, rebuilt by `RebuildKeyGestures()` on init + after Preferences OK); `OnGlobalKeyDown` matches `g.Matches(e)` BEFORE the fixed tool/nav keys and runs `RunKeyCommand(id)`. **Old hardcoded Ctrl switch in OnGlobalKeyDown removed; XAML KeyBindings reduced to just Ctrl+Shift+Z (redo alt); OnKeyDown's Ctrl+K/Ctrl+D cases removed** (keymap owns them). SettingsWindow "Keyboard" page = catalog grouped by category, each row a read-only capture box (tunnel KeyDown → `Canonical(mods,key)`; Backspace=unbind; requires a modifier or F-key so tool letters can't be shadowed) + Reset; **steal+warn** on conflict (`AssignGesture` clears the prior owner, `ChromeWarn` token note). **Tool-cycle letters (B/V/M/...) + raw nav (arrows, +/-/0) stay fixed — NOT rebindable.** Note: menu `InputGesture` labels are static display hints and don't reflect rebinds yet. Verified: 8 tests (GestureFor default/override/unbind, KeyBindings JSON roundtrip, ParseHex) + launch.
- **GPU-tiled layer storage (DONE, Phase 7 §3/§17.3 — atlas + residency + eviction)**: pixel layers no longer keep a monolithic doc-sized GPU buffer; they sample resident 256×256 tiles from a **shared atlas** via a per-layer **tile table**. `Sable.Engine/Compositing/TileResidency` = pure LRU policy (slot map + free-list + linked-list LRU; `Acquire`/`Invalidate`/`ReleaseOwner`/`Clear`; 7 tests). `GpuCompositor`: `_atlasBuf` (one storage buffer capped to `min(device MaxStorageBinding, MaxBufferSize, 768MB)` → `_atlasSlots` of 256 KiB each), `_tileTableBuf` (`[gridW,gridH,slot per tile]`, 0xFFFFFFFF = empty tile, reuploaded per layer per composite), `GetLayerTiles(px)` ensures non-empty tiles resident (uploads dirty/new via `UploadTile`, evicts LRU), `IsTileEmpty` (alpha-scan, cached, invalidated on dirty). **composite.wgsl**: `Params.srcMode` (0=contiguous `src`, 1=atlas), bindings **6=tileTable, 7=atlas** (`_bgl`/bind group now 8 entries); `srcTexel` branches — atlas mode does `tx=ix>>8, slot=tileTable[2+ty*gw+tx], atlas[slot*65536 + (iy&255)*256 + (ix&255)]`. `DispatchBlend(...,tileTable)` binds a 4-byte `_dummyStore` for unused src/mask/table/atlas slots. **Fallbacks/coherence**: monster layers (live tiles > `_atlasSlots`) + the actively brush-previewed layer use the old monolithic `GetLayerBuffer`; atlas path drops a stale cached monolithic buffer when the layer is dirty; residency cleared on doc-swap (`ReleaseLayerCaches`) + resize (`EnsureSize`). **`WgpuDevice` now requests the adapter's full limits (best-effort, non-throwing fallback to defaults) and reports the ACTUAL `DeviceGetLimits` caps** (`MaxStorageBinding`/`MaxBufferSize`) so the atlas never exceeds the real binding limit (default 128MB → 512 slots / 33MP resident). Masks still per-layer contiguous (follow-up). **Recipe: new sampled-source layer = feed a tileTable (atlas) or a contiguous buffer (srcMode=0).** Verified: 7 residency tests + GPU smoke (multi-tile composite, sub-doc/offset masked layer, **dirty-tile re-upload across composites** — paint tile → recomposite → new pixels appear, untouched tiles stay empty) + 190 tests + app launch.
- **Composite-cache (DONE, Phase 7 §7 hot-path)**: brush strokes no longer re-blend the whole stack. `GpuCompositor.CacheHintLayer` (set by the canvas to `ActiveLayer` each composite) marks the edited layer; `CompositeRoot(doc)` snapshots the accumulator BELOW that layer into `_backdropCache` (doc-sized, alloc in EnsureSize) on a full walk, and next composite — if the active layer is the same, the prefix (layers below it) has identical refs/order (`PrefixMatches`) and none dirty (`PrefixDirty`/`SubtreeDirty`) — reuses the snapshot and re-blends only active→top. The big per-layer loop body was extracted to **`BlendOneLayer(layer, ref current, ref other, depth)`**; `CompositeList` (groups) + `CompositeRoot` (root, cached) both drive it. Cache invalidated on resize (`EnsureSize`/`ReleaseSizeResources`) + doc-swap (`ReleaseLayerCaches`). Only caches when the active layer is **top-level with ≥1 layer below** (groups/bottom layer → full walk). Combines with tiling (clean below-layers skip re-upload too). Verified: GPU smoke **`fast==full=True`** (cached path byte-identical to full recomposite) + 190 tests + launch. **Follow-up: region/tile recompositing (only dirty tiles) + viewport-tile culling for true 100MP.**
- **Phase 8 AI — slice 8.0 DONE (infra/seams, no models)**: detailed plan = [PHASE8_AI.md](PHASE8_AI.md). Pure contracts/logic in **`Sable.Core/Ai`** (`AiImage`/`AiMask`/`AiPrompt`/`GenRequest` + 3 model interfaces `IMaskModel`/`IRasterModel`/`IGenerativeModel` + `IAiBackend`; `ModelManifest`+`ModelComponents`+`ComponentSource` model the diffusion component graph incl. shared text-encoder/VAE refs; `ModelCatalog` does component resolution + adapter↔base (LoRA) compat + `VramParts`; `VramGate` = pure sum-vs-peak(offload) decision — "GPU-only" gates COMPUTE not weight staging). **`Sable.Ai`** = `AiService.CheckReadiness`→`AiReadiness`/`AiBlockReason` (NoGpu/NoModel/MissingComponent/WontFitVram), `ModelRegistry` (JSON load/save, `DraftFromFile` heuristics, per-task defaults), `GpuProbe` stub (env `SABLE_AI_FREE_VRAM_MB`; real DXGI/NVML deferred). App: **"AI" top-menu** runs the readiness pre-flight + explains why disabled. **Slice 8.1 DONE (ONNX + Background Removal)**: `Microsoft.ML.OnnxRuntime.DirectML` 1.24.4 in `Sable.Ai`. `OnnxBackend` (DML session host, GPU-only — throws if no DML, no CPU fallback; `CreateMaskModel` factory) · `BiRefNetAdapter` (`IMaskModel`: resize→ImageNet-norm CHW→run→sigmoid mask→resize back) · `Sable.Ai/Imaging/ImageOps` (pure resize/ToChwFloat/MaskFromFloat/CoverageToRgbaMask) · **`SetMaskCommand`** (undoable whole-mask swap) · `AiService.RemoveBackgroundAsync`→command. App **AI▸Remove Background** runs on selected PixelLayer → undoable alpha-matte mask. **Inference NOT headless-verifiable (no bundled weights)** — ImageOps + ORT-load are unit-tested; manual test = drop a BiRefNet onnx + model.json (`adapter:"matte"`,`tasks:["Matte"]`) in `%AppData%/Sable/models/<id>/`. **Model auto-download (DONE, ahead of 8.5; user-asked)**: `Sable.Core/Ai/RecommendedModels` = curated POINTER list (name/url/size/vram/**license**, weights NEVER bundled — ships links only, license shown pre-download; satisfies PLAN §6.3 "download a URL the user chooses") + `Sable.Ai/Download/ModelDownloader` (streams a `RecommendedModel` OR a direct/HF `owner/repo/file` shorthand → `models/<id>/`, progress, auto-drafts model.json, registers). App **AI▸Models** = `ModelsWindow` (recommended rows w/ Download + paste-URL box + installed list). Download itself = network (manual); URL-resolve/catalog/manifest-draft unit-tested (222 total). **AI progress overlay**: `BusyWindow` (borderless centred top-level — **airspace-safe over the native canvas HWND**, unlike an Avalonia scrim) = `BusyWindow.Begin(owner,msg,cts)` + `.Progress` (IProgress<double> → bar 0..1, indeterminate until first Report) + `.SetMessage` + `.Done()` + Cancel→cts. Remove Background uses it w/ a CancellationTokenSource; reuse for every AI op. `BusyWindow` is now **modal (ShowDialog → owner disabled) + full-cover transparent scrim** (separate top-level → dims the native canvas HWND too; airspace-safe). **Slice 8.2 DONE (Upscale + tiling)**: `Sable.Ai/Tiling/TileInference` (pure `Plan`/`Weight`/`Accumulate`/`Finalize` + async `RunAsync`, scale **inferred from first tile output**, per-tile progress) · `EsrganAdapter` (`IRasterModel`, RGB 0..1→factor×) · `ImageOps.Crop`/`ChwFloatToRgba` · `OnnxBackend.CreateRasterModel` · `AiService.UpscaleAsync`→new PixelLayer above source (undoable). App **AI▸Upscale** in modal BusyWindow w/ per-tile progress+cancel. 228 tests; inference manual (esrgan onnx). Doc-resize-to-fit = follow-up. **Model acquisition hardened**: curated catalog now has VERIFIED real URLs (RMBG-1.4 `briaai/RMBG-1.4/onnx/model.onnx` + fp16; Real-ESRGAN x4plus = Qualcomm AI-Hub float-ONNX **.zip** S3 URL); `ModelDownloader` **auto-extracts .zip → finds the .onnx**; `OnnxBackend.GetSession` **rejects non-.onnx** (clear msg: .pth/.safetensors must be exported); `EsrganAdapter` **handles fixed-input models** (Qualcomm export = 128×128 fixed → resize tile→model input→back; `PreferredInputTile()` so `UpscaleAsync` tiles at the model's native size, no quality loss). **URL reachability test** (`RecommendedUrlReachabilityTests`, network — HEAD/range-GET every catalog URL, fails w/ id+status; all 3 live). **Slice 8.3 ONE-CLICK DONE (SAM2 smart-select)**: `Sam2Adapter` (encoder Files[0] embedding-cached + decoder Files[1] per-prompt; decoder inputs name-matched — best-effort, SAM2 export I/O varies) · `Sam2Ops` (pure: doc-px prompts→model-space point_coords/labels, SAM labels 1/0/2/3, centre-point default) · `OnnxBackend` sam2 branch (2 files) · `AiService.SelectSubjectAsync` · App **AI▸Select Subject** centre-point→`SetMaskSelection` in BusyWindow. 232 tests (network excluded). SAM2 needs encoder+decoder onnx pair (`adapter:"sam2"`,`Files:[enc,dec]`). **8.3b Affinity-style hover-to-select DONE**: `Sam2Adapter.SegmentEverythingAsync` (AMG: encode once→decoder over n×n grid→`ObjectMask`s→`AmgOps.Nms`); `Sable.Core/Ai/AmgOps`(pure grid/IoU/NMS/`BestAt`, tested)+`ObjectMask`; `AiService.SegmentEverythingAsync(layer,32)`. **`ToolKind.SmartSelect` in the W selection flyout** (toolbox) — `OnToolChanged`→`StartSmartSelect` precomputes the **active layer** once/layer (BusyWindow). Hover = **diagonal stripes blue(replace)/green(add-Shift)/red(subtract-Alt)** via **blit binding 7 preview R8 tex + `previewMode`** (fullscreen_blit.wgsl now **8 bindings**, VpFloats stays 72 — `previewMode` at u[69]); `GpuSurfaceControl.SmartSelect.cs` reuses `CaptureSelMode`+`ApplyMask`. Selection is **per active layer**. 238 tests; inference+visual = live (SAM2 weights). **Best SAM2 = sam2-hiera-large** (catalog); bilinear mask upscale (smooth, not staircase); NMS bbox-accelerated (ObjectMask carries bbox); progress reserves last 3% for NMS + indeterminate during encode; pan works during SmartSelect (`!_panningMouse` guard); distinct W-flyout icons; **toggling AI off → `ModelRegistry.RemoveAll` deletes all models**.
- **8.4 LaMa object removal (DONE — light tier complete)**: `LamaAdapter` (`IRasterModel`: mask→inpaint, tolerant I/O — 3-chan=image,1-chan=mask; 512 fixed handling; auto 0-255-vs-0-1 output detect; **composites only the masked region** so unmasked pixels stay exact). `OnnxBackend` `lama` branch. `AiService.RemoveObjectAsync(layer,mask)`→inpainted RGBA. App **AI▸Remove Object**: uses `Document.SelectionMask` as the erase region → undoable `RasterStateCommand` (Sable.Tools; apply-then-Capture, Execute idempotent) → clears selection. Full-canvas layer only (offset = follow-up). Catalog **LaMa** (`Carve/LaMa-ONNX/lama_fp32.onnx`, Apache-2.0, 207MB). Combo: Smart Select → Remove Object. **Light tier (bg-removal+upscale+smart-select+object-removal) ships.** **LaMa runs on CPU** (`OnnxBackend.GetCpuSession`) — its Fast-Fourier convolutions (FFC) crash on DirectML (`MatMul`/FFT "parameter incorrect"); **narrow user-approved exception to GPU-only** just for DML-incompatible models, the heavy ones stay GPU. Inference = live (LaMa weights).
- **8.5 model-manager polish (DONE — light tier done; generative tier deferred)**: **VRAM-fit badges** = pure `Sable.Core/Ai/VramBadge.ForModel(vramBytes, freeBytes)`→`VramFit`(Unknown/Fits/Tight/WontFit)+label, reuses `VramGate` so badge+pre-flight agree; shown colour-coded on every recommended+installed `ModelsWindow` row (free VRAM from `Ai.Gpu.FreeVramBytes()`; **0=unknown → requirement-only**, since the real DXGI/NVML probe is deferred to 8.9 — badge auto-lights when the probe returns non-zero). **Per-task defaults** = `ModelRegistry.SetDefault`/`DefaultFor`/`Defaults` (persisted `defaults.json`, already existed) now have UI: `ModelsWindow` "Default model per task" combos (one per task with ≥1 installed base: Matte/Segment/Upscale/Inpaint), `ModelsWindow.DefaultsChanged` event → `MainWindow.ApplyAiVisibility`. 247 tests (+4 `VramBadgeTests`). **Generative tier (8.6 sidecar provision → 8.7 gen-fill → 8.8 outpaint/txt2img → 8.9 contention+real-VRAM-probe) is now a DEFERRED section** in PLAN §18 / PHASE8_AI ("DEFERRED — Generative AI tier"), pulled off the Phase-8 critical path. **8.5 remaining (light): tile-eviction during heavy ops, explicit local-`.onnx` import.**
- **AI opt-in + licensing + model acquisition (DONE — exact UX, do NOT change without asking)**: `SableSettings.AiEnabled` (default OFF) — **AI top-menu hidden until enabled** (Settings▸Machine Learning toggle). **Toggling ON auto-runs `LicenseCycleWindow`** (sequential, one model at a time): fetches each model's ORIGINAL licence untruncated from `LicenseUrl` (HF raw README / project LICENSE) into a `ScrollViewer`+`SelectableTextBlock`, **Accept DISABLED until the user scrolls to the bottom** (forced read), Accept→next / Decline→skip; after the last licence it **installs the accepted models** (in-dialog progress bar). **Declined = skipped**, listed in the ML-panel rows with an "Install" button that re-runs the cycle for that one. No accept = no install. Enable toggle is **live** (`_s.AiEnabled` set in `OnAiEnabledChanged`; `_aiInitializing` guard skips the auto-cycle on ctor-restore); `MainWindow.OnPreferences` always re-applies AI visibility + saves. **Each AI menu ITEM hidden unless its task's model is installed** (`ApplyAiVisibility`→`Registry.DefaultFor(task)`; refreshed after Models/Preferences). Catalog **multi-file** (`RecommendedModel.Downloads: ModelFile[]` + `LicenseUrl`) — **SAM2 = sam2-hiera-tiny encoder+decoder** (vietanhdev, Apache-2.0, enc Files[0]/dec Files[1]); `DefaultSet` = one per task; `ModelDownloader` multi-part + zip-extract. `ModelsWindow` (AI▸Models, advanced/URL): per-model Download (runs the cycle) ↔ Remove + URL paste; `ModelRegistry.Remove`/`IsInstalled`. 234 tests. **Next: 8.4 = LaMa object removal.**
- **macOS AI / WebGPU EP (DONE — Phase 9)**: `OnnxBackend` macOS branch uses the **WebGPU EP (Metal via Dawn)** — `AppendExecutionProvider("WebGPU", new Dictionary<string,string>())` (string API; no typed helper). **No download/build like Linux**: the base `Microsoft.ML.OnnxRuntime` osx-arm64 native already bundles WebGPU AND CoreML — verified providers on M4 = `CoreMLExecutionProvider, WebGpuExecutionProvider, CPUExecutionProvider`. ctor checks `GetAvailableProviders().Contains("WebGpuExecutionProvider")` → `IsAvailable`/`Name="ONNX (WebGPU)"`; readiness then passes (no `EnsureGpuRuntimeAsync` — that's Linux-only). **Apple Silicon only** (pkg ships no osx-x64 native). **Why WebGPU not CoreML (benchmarked on M4, user-decided after seeing the data)**: CoreML's MLProgram path **fails on RMBG** ("unbounded dimension" — needs `COREML_FLAG_ONLY_ALLOW_STATIC_INPUT_SHAPES` to not crash), takes **266s to compile the SAM2-large encoder**, and **fails ESRGAN outright** (ORT EP bug); even working, RMBG on CoreML (817ms) ≈ CPU (813ms). WebGPU runs **all 4 models** (sub-second session-create: sam2-enc 572ms, esrgan 213ms) and RMBG warm-run **362ms = 2.3× faster**; handles dynamic shapes like CUDA/DML (general compute EP, not a graph compiler) so **no per-model flag fiddling**; matches Sable's GPU-first/wgpu philosophy. `GpuProbe` macOS branch (`QueryMac`): `sysctl -n hw.memsize machdep.cpu.brand_string` → `AdapterName`="Apple M4" + unified-memory total; **unified memory has no discrete VRAM** so free=~70% of total as the AI budget (VRAM-fit badges/gate get a real number; `SABLE_AI_FREE_VRAM_MB` env still overrides). Added `_isNvidia` flag so `IsNvidiaPresent` stays false on Mac despite `AdapterName` being set (the CUDA-runtime offer is NVIDIA-only). **CoreML EP is still in the native** (`AppendExecutionProvider_CoreML`) if a future ANE path is wanted per-model; `IsDeviceLost` (DXGI codes) doesn't match WebGPU/Metal so SAM2's GPU→CPU auto-fallback won't trigger on Mac.
- **Linux AI / CUDA EP (DONE — Phase 9)**: ONNX Runtime EP is **per-OS, chosen at runtime** in `OnnxBackend` (managed API exposes every Append* method): **DirectML on Windows, CUDA on Linux, CoreML on macOS**. `Sable.Ai.csproj` references `Microsoft.ML.OnnxRuntime.DirectML` on Windows, base `Microsoft.ML.OnnxRuntime` on Linux/macOS. **Prebuilt ORT has NO Blackwell/sm_120 kernels** (RTX 5090 → "CUBLAS failure 50"); DirectML is Windows-only and the base Linux pkg is CPU-only (no WebGPU EP) — so **no official prebuilt ORT runs on a 5090 on Linux**. Model (chosen w/ user): **Sable builds an sm_120 ORT, the app downloads + activates it** (not per-user compile). `tools/build-ort-cuda.sh [archs] [tag] [cuda] [cudnn]` builds from source (env fixes baked in for CUDA 13 + GCC 16 + system protobuf: `FETCHCONTENT_TRY_FIND_PACKAGE_MODE=NEVER`, `CMAKE_DISABLE_FIND_PACKAGE_Protobuf=ON`, `--compile_no_warning_as_error`, `-allow-unsupported-compiler`, `--no_kleidiai`, `--parallel 6` to avoid OOM) → `dist/onnxruntime-cuda-*.tar.gz`. `Sable.Core/Ai/GpuRuntimeCatalog` = pure metadata keyed by compute arch (**URLs blank until the maintainer uploads + sets Url/Sha256/Size**). `Sable.Ai/Runtime/OrtCudaRuntime` installs to `~/.config/Sable/ort-cuda/` + **activates via `NativeLibrary.SetDllImportResolver(typeof(OrtEnv).Assembly,…)`** so ORT loads our `libonnxruntime.so` (providers load from the same dir) — MUST precede the first ORT call (OrtEnv is a process singleton; `OnnxBackend` ctor activates before `OrtEnv.Instance()`). `OrtRuntimeProvisioner` downloads+extracts (.tar.gz/.zip) or `InstallLocal(dir)`. `GpuProbe` now reports real NVIDIA VRAM + `ComputeArch`/`AdapterName` via `nvidia-smi` (`HasGpu` stays false — readiness gates on a backend's `IsAvailable`). **Provisioning hooks into AI enable** (`SettingsWindow.EnsureGpuRuntimeAsync`, before the model licence cycle, Linux/NVIDIA, skips if installed/no-URL). **`MainWindow` defers building the AI backend until the first AI op** (`ModelReg`/`GpuProbe` are backend-free; Settings/Models/visibility use them; `_aiService` reset after Preferences) so the runtime activates before ORT loads. The sm_120 build links **system CUDA 13 + cuDNN 9** (no redist); a CUDA-runtime-redist provisioner for users without the toolkit is a follow-up (mechanism proven). Verified: built v1.24.4 sm_120, RMBG runs on the 5090 (363 ms); 251 tests. The links the build script downloads (Kitware cmake / ninja) + the maintainer-uploaded ORT archive are network. **The build artifacts on the dev box (`~/ort-build`, ~30 GB) are scratch — safe to delete.**
- **Phase 7 REMAINING: HiDPI** (per-monitor DPI; canvas assumes 1:1) — **DEFERRED** (user: small real-world gain). **Dock.Avalonia docking** — **PARKED** (user). Dock spike findings (see PLAN §18): deps `Dock.Avalonia` + `Dock.Model.Avalonia` (XAML/controls layout keeps panel `x:Name`s in the MainWindow namescope) + theme is a separate pkg `Dock.Avalonia.Themes.Fluent` exposing a `DockFluentTheme` **Styles class** (NOT a StyleInclude path); `DockControl InitializeLayout/InitializeFactory="True"` self-wires. Parked because floating a Tool re-parents into a HostWindow → the bound Colour/Layers panels lose their Window `DocumentViewModel` binding SILENTLY (needs per-Tool DataContext re-point per tab; only live-verifiable). Canvas must stay fixed-centre (native-HWND airspace) when revisited.
- **PSD compatibility audit + report UI (DONE, roadmap Phase 1 §6/§15)**: full audit of the existing `PsdReader` written to **`docs/compat/psd_matrix.md`** — a per-construct matrix (file container / colour mode+depth / compression / layer basics / blend modes / groups / masks / clipping / text / vector+shape / adjustments / layer effects / Smart Objects / artboards / metadata) with SUPPORTED/PARTIAL/IMPORTED_AS_RASTER/UNSUPPORTED labels + per-feature audit blocks (roadmap §19 format: current impl, user-visible problems, proposed impl, required tests, acceptance criteria). **`Sable.Format/CompatibilityReport`** = pure categoriser (raw `PsdReader.Load` warnings+fonts → severity buckets: Rasterised/Partial/Skipped/Structural + MissingFonts); unit-tested. **`CompatibilityReportWindow`** (modeless, `Window▸Compatibility Report`) shows the categorised report persistently; the import toast gains a **"View report"** action that opens it. Report stored per-`DocumentTab` (`CompatibilityReport` field) so it survives after the toast dismisses. **`tests/fixtures/psd/`** corpus = `PsdFixtures` (synthetic valid PSD byte streams, reusing the proven builder shape — no Photoshop needed, deterministic, in git as code) + `PsdFixtureTests` (one test per fixture asserting structure+warnings vs the matrix) + `CompatibilityReportTests`. 489 tests pass. **Recipe for a new PSD construct: add a row to psd_matrix.md + a PsdFixtures factory + a PsdFixtureTests assertion + (if lossy) a warning in PsdReader that CompatibilityReport will bucket.**
- **PSD adjustment-layer mapping (DONE, roadmap Phase 1 §6)**: 15 of 16 PSD adjustment kinds now map to editable `AdjustmentLayer` instead of being skipped. `PsdReader.MappableAdjustmentKeys` (`brit`/`levl`/`curv`/`expA`/`vibA`/`hue2`/`hue`/`blnc`/`blwh`/`mixr`/`clrL`/`nvrt`/`post`/`thrs`/`grdm`/`phfl`) → `ParseLayerInfo` parses the descriptor body into `rec.AdjustmentDesc`; `BuildAdjustmentLayer` + per-kind mappers (`BuildBrightnessContrast`/`BuildLevels`/`BuildCurves`/`BuildHsl`/`BuildColorBalance`/`BuildBlackWhite`/`BuildChannelMixer`/`BuildPosterize`/`BuildThreshold`/`BuildGradientMap`/`BuildPhotoFilter`/etc.) build the `AdjustmentLayer` with params (PS ranges → Sable ranges: e.g. Brightness -100..100 → -1..1, Contrast → 0..2, Hue deg → turns, Curves Hrz/Vrtc 0..255 → 0..1). `phfl` maps approximately to WhiteBalance (filter colour hue → temperature/tint, density → strength); `clrL` maps to ChannelMixer like `mixr`. `selc` stays skipped with warning. Adjustment keeps PSD opacity/visibility/clipping/mask. Fixtures: `AdjustmentBrightnessContrast`/`AdjustmentCurves`/`AdjustmentInvert`/`AdjustmentPhotoFilter`/`AdjustmentSkipped`(selc). **Recipe: new adjustment mapping = key in MappableAdjustmentKeys + a BuildXxx mapper + fixture + test.**
- **PSD SoCo+vmsk → editable PathLayer bridge (DONE, roadmap Phase 1 §6)**: a solid-colour fill layer with a **single closed** vector mask imports as an editable `PathLayer` (fill colour × preserved bezier knots); **multi closed contour** also bridges to `PathLayer` (primary = first contour, rest = `ExtraContours` for even-odd holes); **open contour** falls back to `RasterizeCoverage`→`PixelLayer`. `ParseVectorMaskKnots` (refactored out of `ParseVectorMask`) preserves the anchor + in/out handles (doc px); `PathNode` 6-arg constructor (anchor + handles; `Smooth` auto-detected). Fixtures: `SolidFillShape` (single→PathLayer) + `SolidFillMultiContour` (multi→PathLayer+ExtraContours). **Recipe: new SoCo path = ParseVectorMaskKnots + BuildSoCoLayer branch.**
- **PSD text fidelity warnings (DONE, roadmap Phase 1 §6)**: multi-style TySh runs emit `"text layer has N style runs — flattened to first style."`; text warp emits `"text warp not imported (flattened to un-warped text)."`; vertical text emits `"vertical text not imported (flattened to horizontal)."`; baseline/super-sub/small-caps/all-caps/OpenType each emit their own warning. `PsdTextInfo.Notes` carries them; copied to `rec.Notes` → `CompatibilityReport` buckets them. Fixtures: `TextMultiStyle` + `TextVertical`. **515 tests pass.**
- **PSD layer-effect fidelity warnings (DONE, roadmap Phase 1 §6)**: gradient overlay with >2 stops emits `"gradient overlay has N stops — flattened to first/last."`; bevel/emboss with contour emits `"bevel/emboss contour curve not imported."` + texture emits `"bevel/emboss texture not imported."`. `ParseLfx2` now takes `LayerRecord` to attach notes. Fixtures: `GradientOverlayMultiStop` + `BevelWithContour`. **534 tests pass.**
- **TIFF export (DONE, roadmap Workstream 6 §11)**: `Sable.Imaging/ImageCodec.EncodeTiff` = self-contained baseline uncompressed RGBA8 TIFF encoder (little-endian IFD, 12 tags, RGBA 8-bit, no compression, 72 DPI rational). No Skia/external dependency — pure byte assembly; widely readable by Photoshop/Affinity/GIMP/ImageMagick. `ImageFormat.Tiff` added to the enum + `Extension`/`FormatFromExtension`; `EncodeScaled` routes TIFF through `EncodeTiff` (after resize/flatten); `ExportDialog` format combo gains TIFF; `DocumentIO.Export` + the save picker pick up the `.tif` extension automatically. JPEG/WebP export already existed via Skia. **Recipe: new export format = add to `ImageFormat` enum + `Extension`/`FormatFromExtension` + an encoder branch in `EncodeScaled` + an `ExportDialog` combo item.** Tests: `ExportFormatTests` (17: TIFF header/dims/alpha/resize, enum+extension mapping, JPEG flatten-over-white, TIFF via EncodeScaled).
- **Clipping-mask fidelity — clip to BASE alpha (DONE, roadmap Workstream 1 §7 / Phase 1)**: clipped layers (`ClipToBelow`) now clip to the **base layer's standalone transparency** (PS semantics), not the running backdrop. **Bug was**: `composite.wgsl` `params.clip` multiplied coverage by `dst.a` (the whole accumulator below), so an opaque layer beneath the base (a background) made backdrop alpha ≈ 1 everywhere → the clip leaked across the canvas. **Fix**: `params.clip` is now 3-state (0 off / 1 backdrop-alpha [nested children] / 2 base-alpha [PS clip chains]); new binding 8 `clipBase` (BGL+DispatchBlend now 9 entries) holds the base's standalone coverage, stamped into `_clipBase` over a permanently-zero `_zeroBuf` by `StampClipBase` (own content+mask+transform, FX/opacity excluded). New `GpuCompositor.BlendLayerSequence(layers,start,…,snapshotAt)` replaces the per-list `foreach` in `CompositeRoot` (full + cache-fast paths) / `CompositeList` / pass-through groups: it tracks the clip base (`FindClipBase` = nearest non-clipped layer below the run), stamps once per run, sets the shared `CurrentClip` field that `BlendInto`/`BlendDocContentWithFx` feed to `WriteBlendParams`. Recursive paths (isolated group, nested-children) save/restore `CurrentClip` around the recursion. Verified: `clip-mask` GPU smoke (background no leak; clip shows only over base) + composite-cache/nested/tiled/sub-doc regressions + 534 tests. **Adjustment layers too**: `adjust.wgsl` gained binding 6 `clipBase` (adjust BGL/DispatchAdjust now 7 entries); the adjustment path feeds `CurrentClip` (3-state) not `adj.ClipToBelow` → a clipped Curves/Levels/Invert clips to the base, not the backdrop. Verified by `clip-adjust` GPU smoke (clipped Invert over opaque red bg: left inverted, right keeps bg). **Recipe: a new clip-aware compute pass = add a `clipBase` binding + 3-state clip (0/1/2) + read `CurrentClip`.**
- **Font matching extracted + tested (DONE, roadmap §8/§3.4)**: the PostScript-name → installed-family rule (decides which PSD fonts report missing + which family text renders with) was duplicated in `PsdReader.MapPsFont` + app `FontInstalled` and untested. New pure `Sable.Format.FontMatcher` (installed list injected → headless): `Norm`/`StyleFlags`/`MatchInstalled` (longest-family-prefix wins)/`IsInstalled`/`Resolve`/`Humanize`. Both call sites use it; +25 `FontMatcherTests`. Detection/fallback/report/toast already existed (`OpenPsdTab` → `report.MissingFonts` + toast); this just deduped + locked it.
- **Colour-profile metadata preservation (DONE, roadmap Workstream 5 — full first slice)**: `Document.IccProfile`/`IccProfileName`. `PsdReader.ParseImageResources` now scans the image-resources section (was skipped) for the embedded ICC profile (resource **1039 / 0x040F**) → `Document.IccProfile`; `IccDescription` reads the profile name from the 'desc' (ICCv2) / 'mluc' (ICCv4) tag, best-effort. Re-embedded on export: **PNG** via `ImageCodec.InjectPngIccp` (iCCP chunk = name+0+0+zlib(profile), inserted after IHDR, CRC32) and **TIFF** via `EncodeTiff(...,icc)` (tag **34675** UNDEFINED). `EncodeScaled`/`DocumentIO.Export` thread `icc`+`iccName`; `MainWindow` export passes `doc.IccProfile`. `.sable` stores it as a `color.icc` zip entry (DocDto `IccProfile`/`IccName`). Tests: `ColorProfileTests` (8 — PSD capture, too-small reject+warn, PNG iCCP round-trip+still-decodable, TIFF tag round-trip, `.sable` round-trip) + `PsdFixtures.IccTaggedDocument`/`SampleIccProfile`. 567 tests. **Working space stays sRGB float — this is metadata fidelity, NOT a colour-managed conversion/soft-proof pipeline. Follow-ups: JPEG/WebP ICC embed, PNG/JPEG decode ICC capture, real profile conversion. Recipe: new embeddable metadata = Document field + PSD-resource parse + per-codec embed + .sable entry + thread through Export.**

## Cross-platform (PLAN §2.1/§2.2)
- **Goal: minimize per-OS code — everything shared except the irreducible bits.** Render loop, compositor, viewport, tool logic, coordinate mapping are all platform-agnostic.
- **`Sable.Canvas/Platform/IPlatformBackend`** is the one seam: `CreateSurface(gpu, handle)` (native window → wgpu surface), `CreateInput()` (native event source), `RaiseTimerResolution()`. `CanvasPlatform.Current` picks `WindowsBackend`, `MacBackend` (NSView→CAMetalLayer→wgpu Metal surface, `MacInputSource`), or `LinuxBackend` (X11, `X11InputSource`) — all real; `UnsupportedBackend` is the fallback for any other OS. `InitGpu` catches `PlatformNotSupportedException` → blank canvas, no crash; chrome still runs. **Adding an OS = one new backend.**
- **Input is platform-agnostic** (verified): `IInputSource` decodes native events → shared `ICanvasInputSink` (`PointerDown/Move/Up/Wheel`, surface coords + `CanvasMods`). `WindowsInputSource` = the WndProc subclass (decode only); all tool logic lives in `GpuSurfaceControl`'s sink impl. **No Win32 P/Invoke remains in `GpuSurfaceControl`** — only in `Platform/Windows*`. A new OS needs only a new `IInputSource` + surface descriptor; wgpu itself is already cross-platform.

## Brush preview (in-stack)
- Live preview = a **stamp dab composited into a COPY of the active layer's buffer** (`_previewBuf`) *before* that layer blends — so erase reveals layers below and paint respects the layer's blend/opacity. NOT drawn on top in the blit (that broke erase → showed transparent). Cursor *ring* is the only blit-overlay part. `GpuCompositor.Preview` (`PreviewDab`); control sets it each frame for Brush/Eraser at the doc cursor and forces a recomposite while hovering; cleared during a stroke and before export.
- Layer GPU buffers need `CopySrc` (for the preview copy) — `GetLayerBuffer` allocates `Storage|CopyDst|CopySrc`.

## Gotchas (learned)
- **Avalonia 12 compiled bindings need `x:DataType`** on the binding scope (Window root + each `DataTemplate`), else `AVLN2100`. 
- **`dotnet build` can report success while XAML is broken** (incremental up-to-date skip leaves stale/no precompiled XAML → runtime `XamlLoadException`). After XAML edits, do a clean build (`rm -rf obj bin`) or trust the runtime launch, not the incremental "0 errors".
- **Do NOT `dotnet build -o <dir>`** for the Avalonia app — custom output path breaks XAML precompile. Run from `bin/Debug/net10.0/`.
- **Overriding Fluent theme resource keys: match the TYPE, not your guess.** Some keys are not the type they sound like — e.g. `SliderPreContentMargin`/`SliderPostContentMargin` are **`GridLength`**, not `Thickness`. A wrong-type override builds fine but throws `InvalidCastException` at runtime during template apply (startup crash, managed `0xE0434352`). Safe keys used: `SliderHorizontalHeight`/`SliderTrackThemeHeight` (`x:Double`), `SliderTrackFill`/`SliderTrackValueFill`/`SliderThumbBackground` (+PointerOver/Pressed) (`SolidColorBrush`).
- **Verify GUI startup via exit code, NOT a pipe.** `Sable.App.exe 2>&1 | head` returns the pipe's exit (0) even when the app crashes — false "alive". Use PowerShell `Start-Process -PassThru` then check `$p.HasExited`/`$p.ExitCode`. A native fail-fast **0xC0000409** (STATUS_STACK_BUFFER_OVERRUN) faulting in `wgpu_native.dll` is usually a **managed bug**: an out-of-bounds `stackalloc[...]` write (e.g. writing `entries[3]` into `stackalloc X[3]`) corrupts the /GS cookie and unwinds through native code. Check stackalloc sizes match the indices written.
- **wgpu surface goes `Outdated` when occluded/resized** (e.g. a modal file dialog over the window). If `RenderFrame` just `return`s on non-`Success` `SurfaceGetCurrentTextureStatus`, the canvas freezes on stale content until a manual resize forces `Configure`. Fix: on `Outdated`/`Lost`, call `Configure(_width,_height)` and recover next frame. (This was the "image only shows after maximize" bug.)
- **Canvas render `DispatcherTimer` must run at `DispatcherPriority.Render`, not the default Background** — at Background it's starved behind input/layout and the GPU canvas sits at ~30fps with 100ms+ freezes. `timeBeginPeriod(1)` alone does NOT fix it (Avalonia's DispatcherTimer ignores the multimedia timer). Use `new DispatcherTimer(interval, DispatcherPriority.Render, handler)`.
- **Swapchain present mode MUST be non-blocking (Mailbox) or the render timer starves ALL Avalonia input** — `Render` priority is ABOVE `Input`, so if the per-tick `SurfaceGetCurrentTexture`/`SurfacePresent` blocks on the vblank (`PresentMode.Fifo`, vsync) the UI thread stalls every tick and clicks get **swallowed app-wide** (buttons/menus/anywhere — intermittent). Fix: query `SurfaceCapabilities.PresentModes` and prefer **`PresentMode.Mailbox`** (triple-buffered, no tearing, no CPU vsync wait); fall back to `Fifo` only where Mailbox is unsupported. `GpuSurfaceControl._presentMode` picked alongside `_format`. (Was the "some clicks swallowed basically anywhere" bug.)
- **Brush/eraser preview = a per-frame dab; only recomposite when it CHANGES.** Recompositing every hover frame full-doc lags large docs. Gate on `NeedsComposite || !Nullable.Equals(dab,_lastPreview)`. (`PreviewDab` is a `readonly record struct` → value equality.)
- **Also pending**: Diffusers sidecar (generative tier). Large-doc paint still full-recomposites per brush move (composite-caching is the follow-up). (Windows/macOS/Linux surface paths + SAM2 ONNX are DONE.)
- F5 in VS Code: `.vscode/launch.json` — default runs `Sable.App`, second config runs `Sable.Gpu.Spike`.

## Build/run
- `dotnet build` (whole solution; finds `Sable.slnx`).
- **`dotnet test`** → `tests/Sable.Tests` (xUnit, 28 tests): Core (BlendMode contract, UndoStack), Engine (Document, layer commands, RasterTiles), Tools (BrushTool paint/erase, StrokeSession paint-undo), ViewportTransform, SableFile round-trip, ImageCodec. Pure-logic only (no GPU). **Add a test here for new pure logic — don't put verification in the spike.**
- Run GPU spike (GPU smoke only: wgpu compute blend + N-layer compositor→PNG): `dotnet run --project src/Sable.Gpu.Spike` → `spike_out.png`, `m1_export.png`.
- Run app shell: `dotnet run --project src/Sable.App`.
- Target: **net10.0** (runtime 10.0.6 present). Shared props in `Directory.Build.props` (TFM, nullable, `AllowUnsafeBlocks`).
- `Sable.Gpu` needs `Silk.NET.WebGPU` + `.Native.WGPU` + `.Extensions.WGPU` (DevicePoll lives in the extension). WGSL shaders embedded from `Sable.Gpu/Shaders/*.wgsl`.

## UI conventions (controls + theming) — IMPORTANT
- **i18n is MANDATORY for user-facing strings (build-enforced).** Every `.axaml` `Header`/`Text`/`Content`/`Title`/`ToolTip.Tip`/`Watermark`/`PlaceholderText` must be `{loc:Loc <key>}` (add `xmlns:loc="clr-namespace:Sable.App.Localization"`), with the key+value in `src/Sable.App/Assets/Locales/en.json` (+ `de.json`). Code-behind strings → `Loc.T("key")` / `Loc.T("key", args)`. A literal user string in `.axaml` **fails the build** (`LocaleDoctor` MSBuild target → `tools/locale-doctor.py --strict`). New locale = drop `Locales/<code>.json` with same keys + `language.name`. See [docs/i18n-decision.md](docs/i18n-decision.md). `Loc` singleton + `{loc:Loc}` markup ext in `Sable.App/Localization/`.
- **Reuse controls, don't reinvent.** Recurring UI patterns live as reusable controls in `src/Sable.App/Controls/`: **`LabeledSlider`** (label + `GradientSlider` + value box — for adjustment/effect params), **`HexColorField`** (swatch + hex box), **`SettingRow`** (label-left + content-right). New panels/dialogs MUST compose these instead of hand-rolling `Grid`+`TextBlock`+`Slider`+`TextBox` again. Add a new control here the moment a pattern appears a 2nd time. (Dialogs predating these — AdjustmentWindow/EffectsWindow — still inline; migrate opportunistically.)
- **Sizing/colours come from `src/Sable.App/Theme.axaml`** (single source of truth). Use `{StaticResource}` for sizing tokens, `{DynamicResource Chrome…}` for chrome colours (so they swap with the theme). Do NOT hardcode new `#FFxxxxxx` chrome colours inline — add a `Chrome*` token.
- **Theming engine**: `Theme.axaml` has `ThemeDictionaries` for Dark / Gray / Light (Gray = `Themes.Gray` custom `ThemeVariant`). `MainWindow.ApplyTheme` sets `RequestedThemeVariant`; chrome surfaces bound via `DynamicResource` re-theme live. New chrome colour → add it to all three theme dictionaries.
- **GOTCHA — resolving `Chrome*` brushes in CODE-BEHIND**: `control.TryFindResource(key, out v)` (the no-variant overload) does NOT resolve `ThemeDictionaries` entries → returns false → your fallback (e.g. `Brushes.Gray`) renders gray-on-gray. When building rows/controls in code, bind the colour to the theme observable instead: `tb.Bind(TextBlock.ForegroundProperty, this.GetResourceObservable("ChromeText"))` (and `Border.BackgroundProperty` etc.) — resolves the active variant AND re-themes live, same as XAML `{DynamicResource}`. (Bit `ModelsWindow` recommended rows.)

## Conventions
- Caveman comms in chat; normal prose in code/docs/commits.
- New effect = graph node checklist: serializable params + WGSL compute pass + undo entry. Destructive-only effect = bug.

Rules in this file apply to every Claude Code session in this repo. They override generic defaults and persist across conversations.

## When unsure, ASK — always

If a request is ambiguous, has more than one plausible interpretation, or you are about to make a non-trivial design/scope decision: **stop and ask the user before implementing.** Do not guess. Do not pick "the most likely" reading and run with it.

**Why:** guessing wrong on a multi-file feature wastes a full build cycle and the user's time, and it has happened repeatedly. A 10-second clarifying question is always cheaper than a wrong implementation.

**How to apply:**

*   Ask BEFORE writing code, not after. One tight, specific question (or a short numbered list of options) — not "should I proceed?".
*   This applies even under time pressure or when the user seems impatient. A wrong big change is worse than a question.
*   Small, reversible, obvious things (a typo fix, an unambiguous one-liner) don't need a question. Anything touching multiple files, the data model, or UX behaviour does if there's any doubt.
*   If the user already answered a question, don't re-ask it — read carefully.

## No emojis

Do NOT add emoji glyphs anywhere — XAML, locale JSON, C# code (labels / Debug.WriteLine prefixes / log tags), JavaScript, prose responses, menu items, ribbon entries, button content, finding-type markers, or any other surface.

This covers all pictographs in the Unicode emoji blocks:

*   `U+1F300`–`U+1FAFF`
*   `U+2600`–`U+27BF`
*   common offenders: `✒ ✂ 💡 🎨 🎭 🔗 📊 📝 🗑 ➤ ⚠ ➕`

**Acceptable visual markers:**

*   SVG path-geometry strings (Lucide-style, e.g. `M21 15a2 2 0 0 1-2 2H7l-4 4V5...`) used in `IconPath` on ribbon items, activity-bar entries, sidebar contributors, ContentViewDescriptor etc. These are the project's icon system.
*   Non-emoji unicode punctuation when needed and no SVG exists: `× ✕ → ←` for close / arrow buttons.
*   Plain text labels — always preferred.

**Why:** user has stated explicitly that emojis make the app feel like dumb consumer software. This was reinforced by removing every emoji previously introduced (inline actions, context menus, story-analysis filters, chat buttons, finding type icons). Treat this as a hard product-aesthetic constraint, not a stylistic suggestion.

**How to apply:**

*   When adding a new menu item, button, ribbon entry, descriptor, or locale string: use a text label and either an empty `Icon` field or an SVG `IconPath`. Never reach for an emoji as a quick visual marker.
*   When touching a file that already contains emojis (in UI, locales, or labels): strip them as part of the change.
*   Do not put emojis in Debug.WriteLine or console.log prefixes either (e.g. avoid `[💡 InlineActions]` — use `[InlineActions]`).
